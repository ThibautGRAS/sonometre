# Sonomètre CETIM — Mémoire du projet

Application web mono-fichier (sonomètre + analyseur) pour iPhone, déployée sur GitHub Pages.
Ce document sert de mémoire technique : architecture, conventions, pièges connus et journal des versions.

- **Repo** : `ThibautGRAS/sonometre` (branche `main`)
- **URL live** : https://thibautgras.github.io/sonometre/
- **Auteur** : Thibaut Gras · CETIM · thibaut.gras@cetim.fr

---

## 1. Fichiers

| Fichier | Rôle |
|---|---|
| `index.html` | Application complète (HTML + CSS + JS en un seul fichier) |
| `sw.js` | Service worker (ouverture hors ligne, gestion du cache) |
| `manifest.webmanifest` | Manifeste PWA (installation écran d'accueil) |
| `icon-192.png`, `icon-512.png` | Icônes d'application |
| `README.md` | Présentation publique |
| `MEMOIRE.md` | Ce document |

## 2. Déploiement

Déploiement via l'API GitHub Contents (`api.github.com`), pas de build local (travail sur iPhone).
À **chaque** déploiement :
1. Ré-estamper le cache du service worker : `const CACHE = 'sono-<hash8>'` (hash du `index.html`).
2. Mettre à jour le numéro de version affiché sur le splash (`.splashver`) et l'entrée du journal ci-dessous.
3. Valider le JS avant envoi : extraire le plus gros bloc `<script>`, `node --check`, vérifier que chaque `$('id')` a un `id="..."` correspondant.
4. Recharger l'app sur l'appareil pour récupérer la nouvelle version.

## 3. Conventions

- **Versionnage `MAJEUR.MINEUR.PATCH`** (depuis 1.35.44) : la V1 couvre l'application actuelle ; passage à **2.x** au prochain changement majeur. Les entrées historiques « V35.x » du journal équivalent à « 1.35.x ». **Source unique de version = le splash** (`#splashVer`, bumpé à chaque déploiement) ; le JS la lit (`APP_VERSION`) et l'affiche aussi dans le titre de l'aide (`#hpVer`).

- **Charte CETIM** : marine `#001E50`, rouge `#EF3346`, police Arial Nova. Logo vectorisé (traçage de contours) inclus en SVG.
- **Thèmes** : Marine (défaut CSS), Acier, Clair labo, OLED noir (défaut applicatif), Game Boy DMG-01, Matrice. Tout suit le thème via variables CSS.
- **Persistance** : préférences dans `localStorage` (local à chaque appareil/navigateur, **jamais** sur Git) sous `sono_prefs` ; langue sous `sono_lang` ; profils de calibration sous une clé dédiée. Survit aux mises à jour, effacé si l'utilisateur vide les données du site.
- **Langues** : FR (source), EN, ES — bascule par drapeaux en tête de l'aide, dictionnaire `TR` + `translatePass()`.

## 4. Pièges techniques connus

- `CV.sgramCv` est un objet `{g,w,h,dpr}`, **pas** un canvas — dessiner en coordonnées logiques.
- `SG.cols` est un tampon **décalé** (le plus récent toujours à `Wp-1`), pas circulaire.
- Ligne 0 du tampon spectrogramme = fréquence **haute**.
- Le fichier contient **deux** blocs `<script>` ; le module i18n est injecté à la fin du plus gros.
- Échelle couleur du spectrogramme **partagée** entre vues 2D et 3D (`S.scale.sgram`).
- `autoRange()` : anti-scintillement (expansion immédiate, contraction temporisée 2 s).
- **Démarrage sans saut** (important, source de plusieurs bugs passés) :
  - thème appliqué par un mini-script inline **avant** le premier rendu (sinon flash marine→thème réel) ;
  - écran + clavier masqués (`body.booting`) puis révélés après le premier rendu complet ;
  - splash screen par-dessus, fondu après durée minimale ;
  - **pas** de `location.reload()` automatique sur `controllerchange` (il provoquait un double-chargement visible à chaque mise à jour).
- Service worker : `updateViaCache:'none'` à l'enregistrement + `fetch(req,{cache:'reload'})` pour le document → la version fraîche est servie sans dépendre du cache HTTP.
- Icônes flottantes des graphes (`.sg3dbtn`) : taille uniforme 46×26, estompage automatique après 3,5 s d'inactivité.
- Traduction : ne jamais écraser le contenu d'une touche qui contient un SVG (icônes ▶ ■ ⏸) — garde `if(el.children.length)return`. Valeurs composées traduites mot à mot (`Tstr`), pas par correspondance exacte. **Valeurs dynamiques** (touches `.kv`, valeurs `.mrow b`) : traduites via `trDyn()` qui traduit TOUJOURS depuis le français mémorisé (`dataset.fr`) avec garde `dataset.tr` (ne retraduit jamais un texte déjà traduit par nous). Sans ça, le remplacement mot-à-mot recompoundait (« Expert »→« Experto »→« Expertoo »… car « Expert » est un sous-mot de « Experto ») et la bascule ES→EN restait bloquée (on traduisait depuis l'espagnol au lieu du français).
- Spectrogramme 3D : réduire une tranche de fréquence par **max**, pas par moyenne (une raie fine serait diluée) ; lisser en **préservant les maxima locaux** (sinon le 1-2-1 rabaisse les raies). Les valeurs 3D doivent correspondre à la 2D.
- **Mouvement continu 3D (état actuel, V35.18→36)** : `drawSgram3D` n'échantillonne plus le tampon à âges fixes (ça faisait SAUTER les crêtes). Modèle **buffer de courbes-instantanés glissantes** dans `window.V3={frames,ema,acc,lastT,sig}` :
  - **EMA continue (V35.29)** : la colonne live est lissée à CHAQUE image (constante de temps ~55 ms, `a=1-exp(-dt/55)`) ; le **front** (dernier frame) est resynchronisé sur l'EMA chaque redraw (`frames[last].set(V.ema)`) → sa forme morphe en continu (fini le saut vertical du présent). Un instantané figé est poussé à `capInt=winSec*1000/(maxFrames-1)` (min 16 ms) ; `frames` plafonné à `maxFrames` (**96**, plus de mode Fluide/surface).
  - profondeur **continue** : `dzArr[k]=1-((len-1-k)+phase)/(maxFrames-1)`, `phase=acc/capInt` (0→1) ; front `dz≈1`.
  - `sig` = signature (mode, nF, fmin, fmax, maxFrames) → **reset des frames** si la vue change ; `resetSg3d()` vide `V3` sur clearAll/clearSgram/reset mesure (sinon on revoit l'ancienne trace).
  - **cache** : `key` (chaîne) contient `run/stop` et non `seq` → redraw **chaque frame en marche**, figé à l'arrêt.
  - **cadence ~60 FPS** (`SG._3dLast`, garde 10 ms) — le rendu est allégé (1 tracé dégradé par courbe, 1 point sur 2). **Anti-scintillement (V35.28)** : fondu du bord arrière (`edgeA` selon `dz`, la plus ancienne s'estompe au lieu de « poper »).
  - **Perf mesure longue (V35.30)** : pool de tampons persistant `SG._sc={pool,slice,norm,raw,dz}` réutilisé chaque frame → plus d'allocation dans la boucle chaude (fin des à-coups GC). `smoothProf(prof,out)` écrit dans un tampon fourni.
  - **3 niveaux de lissage (V35.30)** : `S.sg3dSmooth` 0-2 = Standard/Lisse/Très lisse (défaut Lisse) → `wirePass=smoothLvl` (passes 1-2-1 fil de fer, pics préservés).
  - **Axe temps (V35.31)** : plus de marqueur « ▲ présent » — un axe temps sur la profondeur (étiquettes `0 s`/`-winSec/2`/`-winSec`, titre « temps », `marginL=38`).
  - **Rotation** : azimut `S.sg3dRot` (`skewTot=plotW*(0.12+0.50*rot)`, symétrique), tangage `S.sg3dPitch` (`depth=plotH*(0.40+0.48*pitch)`, geste vertical `dy/220`).
- **Curseur FFT (V35.35-37)** : posé au tap ; **accroche un pic local** (fenêtre **±4 %** ou ≥3 bins, élargie en V35.37, + interp. parabolique) SEULEMENT si le tap est **au-dessus** de la courbe (`S.cursor.snap`, décidé via `tapDb > specCurveDb(f)+1`), sinon placement libre. **Déplaçable au glissé** (prise à 5 % de largeur, `mode='cursor'` dans `attachZoomPan`, conserve `snap`). **Double-tap dessus** = l'enlève. Zoom vertical dB **ancré sous les doigts** (`anchors.yc`) → plus de dérive.
- **Deux chemins de rendu (état actuel)** : le glissement continu ne s'applique qu'aux fenêtres **bornées 3/5/10 s** (`!isTout`, seules proposées en 3D). Le mode **« Tout »** (`isTout`, réservé à la **2D** depuis V35.27) garde l'**ancien rendu décimé** (`maxSlices=90`, tranches à âges fixes, `dzArr[j]=j/(nT-1)`, cache par `seq`). `smoothProf` est partagé. Le **mode surface a été entièrement retiré** (fil de fer uniquement).

## 5. Fonctions principales

- **Écrans** : Spectre (tiers d'octave / octave / FFT bande fine), Spectrogramme (2D + vue 3D waterfall), Évolution temporelle, Valeurs (2 pages).
- **Indices** : LAeq, LCpeak, LAFmax/min, L10/L50/L90, LAE (SEL), Δ LF.
- **Émergence tonale** (ISO 1996-2) : toujours évaluée ; affichée dans le tableau de valeurs et au curseur FFT ; seuil réglable au menu expert ; bouton ÉMG = encadré orange de détection auto sur la bande fine.
- **VIB** — corrélation vibro-acoustique : coefficient de Pearson (0→1) entre l'enveloppe de chaque bande et le niveau accéléromètre (téléphone posé sur la structure). Corrèle les enveloppes (±250 ms de délai toléré), pas les formes d'onde ; une source constante n'est pas corrélable. Outil de tri « ce bruit vient-il de cette machine ? ». Adaptation « domaine enveloppe » de la méthode de cohérence (coherent output power, Bendat & Piersol).
- **Écoute filtrée (🔊)** : réinjection du micro vers la sortie à travers un passe-bande calé sur la bande de fréquence zoomée du spectrogramme, pente 24/48/96 dB/oct réglable. Écouteurs indispensables (larsen). Latence mesurée affichée.
- **Calibration / réf. pleine échelle (0 dBFS)** : `S.offset` = niveau correspondant à 0 dBFS (≈120 dB ; **pas** une « correction d'amplitude », terme corrigé partout en V35.36). Réglage au calibreur (94/114 dB) + correction fréquentielle par tiers d'octave face à un sonomètre étalon, enregistrable en profil.
- **Export / Rapport** (menu EXPORT) : trois blocs. (1) **Infos du rapport** facultatives mémorisées (`localStorage` `rep_repSite/Oper/Ref/Note` + cases figures `rep_figSpec/Sgram/Hist`). (2) **Rapport (PDF)** : `generateReport()` ouvre un **onglet autonome** (`window.open`+`document.write(buildReportDoc)`, styles `REPORT_CSS`, impression auto au `load` → « Enregistrer en PDF » iOS ; repli partage `.html` si popup bloqué). Contenu : **logo CETIM** (`cetimLogoSVG`, reconstruit depuis le SVG du DOM), indices, conditions d'acquisition (dont **Réf. pleine échelle 0 dBFS** = `S.offset`), **figures cochées** rendues à la demande même hors écran (`figureDataURL` → `drawSpectrum`/`drawSgram`/`drawHistory` puis `toDataURL`), spectre 1/3 oct, commentaire, pied non-métrologique. (3) **CSV** complet et **PNG** titré (en-tête avec logo `LOGO_IMG_LIGHT` pré-rasterisé), via la feuille de partage iOS.

## 6. Transport (boutons)

Convention **enregistreur** (comme le Dictaphone iOS) : bouton principal **rouge** avec point d'enregistrement ● au repos → carré ■ + halo pulsé en mesure. Pause dédiée ⏸ (ambre vif « enfoncé » quand active), ne se métamorphose jamais en triangle.

## 7. Points en attente / à faire

- **CRITIQUE** : révoquer le jeton GitHub (PAT) utilisé pour les déploiements — il a été exposé en clair. GitHub ▸ Settings ▸ Developer settings ▸ Personal access tokens (révoquer + recréer un fine-grained limité au repo `sonometre`, permission Contents R/W).
- Rapport PDF : `window.open` peut être bloqué en mode « app écran d'accueil » (standalone) → repli sur partage `.html`. À valider si Thibaut installe l'app.
- Logo : logo d'en-tête ramené à 15 px (aligné sur le wordmark « Sonomètre ») en V35.37 ; restent le logo du rapport (32 px) et l'export PNG (18 px), à confirmer/affiner selon le rendu voulu.
- Passe 2 de traduction : toasts éphémères et étiquettes dessinées dans les graphes (encore en français). Aide FR/EN/ES du bouton MOY : faite (V35.42).
- VIB : **retiré de l'interface** (V35.42), code conservé et réactivable (rétablir l'affichage de `#vibBtn` dans `ui()` + l'entrée HTML). Décision finale garder/supprimer selon l'usage terrain.
- Écoute filtrée : à tester sur un cas d'identification de raie réel.

---

## 7bis. Validation du traitement du signal (banc de tests)

Dossier `tests/` du dépôt — **à rejouer après toute évolution du code DSP** :
`node extract_dsp.js` (extrait les fonctions depuis `index.html` → on teste le code déployé) puis `node run_tests.js` (61 tests vs références analytiques/normatives IEC 61672-1, IEC 61260-1, Parseval ; sortie console + `results.json`, code retour 0 = OK). Rapport détaillé : `make_report.js` → docx format CETIM (réf. SONO-VAL-001). Écarts documentés : pondération C **+0,65 dB à 8 kHz** (bilinéaire, fs=48k, sans effet sur LCpeak) ; **sommet de raie fine −10·log10(ENBW)** (normalisation Parseval : bandes et globaux exacts, sommet ponctuel étalé). Hors périmètre : chaîne micro iOS, calibration absolue, émergence ISO 1996-2 complète (validation sur appareil).

**Procédure GLOBALE (une commande)** : `python3 tests/make_global_report.py` régénère, à partir du code déployé, les 3 bancs + les courbes + le rapport global .docx/.html (réf. SONO-VAL-000 = Partie I SONO-VAL-001 + Partie II SONO-VAL-002). C'est la procédure de validation de référence, à rejouer avant toute fusion V2→V1.

**Temps réel (comparaison des méthodes)** : `tests/test_realtime.js` (`node tests/test_realtime.js`) rejoue la boucle bloc par bloc et compare LAeq/LCeq/LZeq entre IIR temporel, dérivé FFT et théorie, sur tons/multiton/bruit rose, correction Off et MEMS. Constat validé : **IIR ≡ théorie à ≤0,22 dB** (Off et correction) ; le dérivé FFT diverge sous correction (cancellation de Parseval) — normal, d'où l'Option 1. À rejouer après toute évolution des chaînes de niveau.

**Pondérations A/C (gabarit)** : `tests/test_weight.js` (`node tests/test_weight.js`) compare les biquads A/C aux valeurs nominales IEC 61672-1 et aux tolérances classe 1/2. Résultat validé : **classe 1 de 16 Hz à 12,5 kHz** (écart ≤ 0,8 dB) ; dégradation au-delà (limite bilinéaire près de Nyquist, sans impact sur les niveaux pondérés).

**Routine dédiée (filtre de correction)** : `tests/test_corr.js` (`node tests/test_corr.js`, code retour 0=OK) extrait les fonctions réelles de `index.html` et exécute 49 contrôles : fidélité magnitude (4 courbes), neutralité Off, gain réel sur tons purs, cohérence énergétique Z, cohérence chaîne corr→A, stabilité, référence A. À rejouer après toute évolution du filtre.

**Ajouts à valider (filtre de correction micro temporel, Option 1) :**
1. **Fidélité du filtre** : pour un profil de correction donné, comparer la magnitude de la cascade de biquads (`designCorrFilter`) à la courbe cible sur 16 Hz–Nyquist. Critère : **≤ ~0,5 dB de 50 Hz à 12,5 kHz**, ≤ ~1 dB au coin extrême grave (~31 Hz), bord >16 kHz non contraint (hors bande A/C). (Courbe MEMS type +10 dB grave→plat→chute HF : validé maxErr bande utile 1,03 dB @31 Hz, <0,5 dB 50 Hz–12,5 kHz, fs 44,1/48 k.)
2. **Cohérence niveaux ↔ Leq** : WAV de référence (bruit rose + tons) passé dans la chaîne temporelle corrigée ; vérifier LAF/LAS/LV, LCpeak et **LAeq/LCeq/LZeq** vs valeurs théoriques (spectre corrigé). LAF ↔ LAeq doivent concorder (même signal filtré).
3. **Cohérence spectre ↔ niveaux** : le spectre (corrigé par `compGain` en fréquence) et les niveaux (corrigés par filtre en temps) doivent donner le même écart global vs non corrigé (à la fuite spectrale près).
4. **Neutralité Off** : correction=Off → LAF/LCpeak/Leq **identiques** au comportement historique (filtre=identité, Leq=FFT/Parseval).

## 7ter. Chaîne de traitement du signal (V2) — schéma de référence

Deux chaînes parallèles à partir du même signal micro :

- **Chaîne FFT (fréquentielle)** : `ring` fenêtré → FFT → `mag[]`. Correction micro appliquée **raie par raie** (`compGain[i]`, si corr≠Off). Sert : **spectres** (bande fine `binPowD`, tiers/octave `bandPowD`, spectrogramme), l'**émergence**, et le **Leq quand correction=Off** (sommes pondérées `pA/pC/pZ` recalées par **Parseval** `K=Pt/pZ` sur le RMS temporel brut).
- **Chaîne temporelle (échantillon par échantillon, `lvlDet`)** : échantillons neufs → **filtre de correction** (cascade biquads `designCorrFilter`, si corr≠Off) → pondération **A/C** (biquads IEC 61672) → carré → intégrateurs exponentiels **Fast/Slow/V-Slow** + **crête C** (LCpeak). Sert : **niveaux instantanés** LAF/LAS/LV, LAFmax, LCpeak, **et le Leq quand correction≠Off** (énergies cumulées du signal **corrigé** : `det.eqA/eqC/eqZ/eqN` → `S.eq`).

**Application de la correction — récapitulatif :**
| Correction | Spectres | Leq (LAeq/LCeq/LZeq) | Niveaux temps réel (LAF…, LCpeak) |
|---|---|---|---|
| **Off** | non | FFT + Parseval (Z=RMS brut) | temporel brut |
| **Active** | oui (compGain, par raie) | **temporel corrigé** (signal filtré) | **temporel corrigé** (signal filtré) |

**Offset scalaire** (calibreur) : appliqué partout, dans tous les cas (`S.offset`).

**Amplitude FFT & fenêtre.** L'amplitude spectrale est recalée par **identité de Parseval** : `K=Pt/pZ` (Pt = RMS temporel du bloc **fenêtré**, pZ = somme des raies). Numérateur et dénominateur portant la même fenêtre, la perte de puissance de la fenêtre **s'annule** → niveau juste quelle que soit la fenêtre (pas de constante par fenêtre). Conséquence : niveaux de **bande / tiers / Leq exacts** (énergie intégrée). **Raie tonale en bande fine** : le sommet tracé est sous-estimé de `−10·log10(ENBW)` (≈ −2,4 dB Blackman) car le lobe étale le ton ; la **lecture au curseur** est corrigée (snap au max + interpolation parabolique du sommet → vraie fréquence/niveau). **Tiers d'octave** : synthèse FFT (somme d'énergie, exacte via Parseval) OU banc IEC 61260 (filtres Butterworth par bande, sans fenêtre).

**Détails d'implémentation** : filtre de correction reconstruit dans `configureAnalyser` (en place, `S.lvlDet.corr=buildCorrFilter(fs)`, sans reset des intégrateurs) à chaque changement de courbe/corr ; `lvlDetRun(det,x,acc)` accumule le Leq seulement si `acc=!idle` ; bascule FFT↔temporel via `tdLeq=(S.corr!=='off')&&!REPL.on`. Replay : Leq issu des cumuls stockés (`F.cumA/C/Z`), chaîne inchangée. Correction verrouillée pendant la mesure (pas de bascule à chaud). Repli FIR (courbe très accidentée, `maxErr` élevé) : non implémenté — les courbes MEMS visées restent dans le domaine des biquads.

## 3bis. Branche V2 (chantier enregistrement/replay)

- **Branches** : `main` = V1 stable (1.35.x), PWA du quotidien, intouchée. **`V2`** = développement enregistrement audio + replay, versions **2.0.x-beta**. Fusion V2→main seulement si le replay fait ses preuves ; critère objectif = banc `tests/` + WAV de référence bout en bout (chantier 5).
- **Canal de test iPhone** : GitHub Pages ne sert que `main` → le build de V2 est copié dans `main:/beta/index.html`, testable sur `…/sonometre/beta/`. Le beta n'enregistre **pas** de service worker (`location.pathname.includes('/beta/')`), et le SW de main **exclut** les chemins `/beta/` de son scope (sinon il écraserait le cache V1 avec la page beta et servirait la V1 hors-ligne sur /beta/).
- **Plan V2** : ① capture PCM + WAV (fait, 2.0.1) → ② bibliothèque IndexedDB (fait, 2.0.2) → ③ moteur de replay via AudioBufferSourceNode→analyser (fait, 2.0.3) → ④ timeline/scrub (fait, 2.0.4 ; vitesse variable écartée : fausserait l'analyse) → ⑤ WAV de référence dans le banc.

## 8. Journal des versions (V2)

- **2.2.26-beta** : **Moyenne glissante = fidèle instrument (pondération F/S en énergie)**. L'utilisateur veut le comportement d'un vrai appareil. Repassé l'EMA du spectre en **domaine énergie** (moyenne exponentielle du carré de pression = pondération temporelle IEC 61672), et remplacé les constantes par les **standards Fast 125 ms / Slow 1 s** (+ 5 s pour un spectre lissé). binEma/bandEma = EMA énergie (affichage) ; binEmaDb/binEmaDb2 = EMA dB (moyenne+ordre 2) pour l'IC glissant. Décroissance −4,34 dB/τ (donc Fast redescend vite, Slow posément — exactement comme un sonomètre ; le « traînage » perçu était le comportement normal en énergie, juste avec un τ trop long). Cumulée (Leq) inchangée pour le rapport. Cycle avgTau = [0, 0.125, 1, 5]. Menu/ui/aide MAJ. node --check + loadtest OK. RESTE : sonie temps-variable ; promotion beta→prod.

- **2.2.25-beta** : **CORRECTIF clé — moyenne glissante en dB (niveau), plus en énergie**. Cause du « le sifflet met très longtemps à redescendre même en τ=2 s » : l'EMA se faisait en PUISSANCE, or une décroissance exponentielle en puissance ne vaut que −4,34 dB/τ → un pic 40 dB au-dessus du fond mettait ~18 s à redescendre visuellement à τ=2 s. Basculé l'EMA en **domaine dB (niveau)** : binEma et bandEma stockent désormais l'EMA du niveau L=10log10(p) ; l'affichage MOY glissante reconvertit 10^(L/10) en puissance avant pondération/lvl. Résultat : 63 % de la chute en dB en τ, ~95 % en 3τ (vérifié : 90→40 dB revient à ~43 dB en 5 s à τ=2 s). CI en mode glissant : variance dB glissante via binEma (moyenne) + binEmaDb2 (ordre 2) ; binEmaDb supprimé (redondant). Tiers/octave idem. node --check + loadtest OK. RESTE : sonie temps-variable ; promotion beta→prod.

- **2.2.24-beta** : **Correctif IC de la MOY en mode glissant (oubli cohérent)**. Bug : la bande de confiance calculait σ sur les stats CUMULÉES (binSumDb/binSumDb2 sur tout eqT) même en moyenne glissante τ → après un sifflet, le trait EMA redescendait mais la bande restait gonflée à cette fréquence (ne « oubliait » jamais), donnant l'impression d'un pic persistant. Ajout de stats dB GLISSANTES par bin (binEmaDb, binEmaDb2, EMA coeff a=dt/τ, seed S._emaSeed, reset avec les autres). Au tracé : si avgTau>0 → σ issu de la variance glissante (oublie en ~τ) ; sinon → σ cumulé (Leq). La bande suit donc le mode de moyenne. node --check + loadtest OK. RESTE : sonie temps-variable ; promotion beta→prod.

- **2.2.23-beta** : **Réglages spectre regroupés dans Signal + renommés**. mAvgTau et mSpecCI déplacés de l'onglet Affichage → **Signal** (ce sont du traitement du signal). Renommage clair et parallèle des 4 réglages : « Moyenne temporelle — live (trames, Welch) » (specFrames, court terme sur l'INST), « Moyenne temporelle — MOY (Leq / glissante τ) » (avgTau, long terme sur la MOY), « Lissage fréquentiel — bande fine (1/n oct) » (specSmooth, agit en fréquence), « Intervalle de confiance de la MOY (±2σ/√N) » (specCI). Aide alignée (noms + Expert ▸ Signal). Aucun changement de logique. node --check + loadtest OK. RESTE : sonie temps-variable ; promotion beta→prod.

- **2.2.22-beta** : **Moyenne du spectre : mode glissant exponentiel (option)**. Constat : la MOY (spectre fin + tiers/octave) est une moyenne d'énergie CUMULÉE depuis le début (binEnergy/eqT, bandEnergy/eqT = Leq à poids égal) → de plus en plus inerte (perçue « trop lente »). Ajout option experte `S.avgTau` (Affichage, #mAvgTau : Cumulée(Leq) → τ 2 → 5 → 10 s, persistée) : moyenne glissante exponentielle par bin/bande (binEma/bandEma, coeff a=dt/τ, seed sur 1ʳᵉ trame via S._emaSeed, reset avec les cumuls). En τ>0 l'affichage MOY (fin + tiers/octave) lit l'EMA au lieu du cumul ; le Leq cumulé reste pour le rapport. IC de la moyenne : Neff calculé sur la fenêtre effective (τ si glissante, sinon eqT). Vérifié : EMA→0.633 à t=τ (1−1/e). Aide MAJ. node --check + loadtest OK. RESTE : sonie temps-variable ; promotion beta→prod.

- **2.2.21-beta** : **La bande devient l'IC de la MOYENNE (se resserre)**. Retour utilisateur : le ±2σ de dispersion ne bougeait pas dans le temps (normal : c'est la variabilité du signal, elle converge mais ne rétrécit pas). Basculé sur l'**intervalle de confiance de la moyenne** : demi-largeur = 2·σ/√Neff avec Neff = eqT / (fftN/fs) = nb de fenêtres indépendantes (Welch) accumulées → la bande se resserre ∝ 1/√durée. σ toujours calculé depuis binSumDb/binSumDb2 (dB par bin). Libellé menu → « Intervalle de confiance de la moyenne (spectre) » ; aide précisée (distinction IC de la moyenne vs dispersion ±2σ). node --check + loadtest OK. RESTE : sonie temps-variable ; promotion beta→prod.

- **2.2.20-beta** : **Bande de confiance ±2σ sur le spectre fin moyenné**. Nouvelle option experte (Affichage) `S.specCI` (défaut Non, persistée) : en mode MOY, trace en fond translucide (fill THEME.line α0.15 + liserés α0.4) l'enveloppe niveau ±2σ de la variabilité TEMPORELLE par bin. Implémentation : accumulateurs par bin `S.binSumDb`/`S.binSumDb2` (Σ dB·dt, Σ dB²·dt) remplis en même temps que binEnergy (dB = 10log10(p) sur le spectre corrigé, avant pondération → σ indépendant de l'offset et de A/C), dénominateur = S.eqT. Au tracé (branche fine de drawSpectrum) : σ_bin = √(ΣdB²/T − mean²), lissé ±3 bins, bande = courbe_affichée ± 2σ (dB), incluse dans l'autoRange, gate ciOn = specCI && useAvg && !REPL && eqT>0.5. Reset avec binEnergy. Menu (#mSpecCI Oui/Non) + ui + onclick + aide. node --check + loadtest OK. RESTE : sonie temps-variable ; promotion beta→prod.

- **2.2.19-beta** : **Repère visuel « fluctuation en cours d'établissement »**. Tant que le tampon glissant n'atteint pas PSY_BUFSEC=5 s (flag fProv = ready && !avg && span<5), l'axe Fluctuation est marqué : point du polygone **creux** (ring) au lieu de plein, tic P95 en **pointillé**, « … » près du libellé, et si l'axe est sélectionné, ligne « fenêtre glissante en cours : x.x / 5 s — valeur provisoire » (encart agrandi). Confirmé au passage que le seuil d'affichage MIN=2 s est intact (délai d'affichage de la toile, distinct de la fenêtre de fluctuation). Aide MAJ. node --check + loadtest OK. RESTE : sonie temps-variable ; promotion beta→prod.

- **2.2.18-beta** : **Précision fenêtre de fluctuation dans l'aide**. Question utilisateur : fluctuation « 5 s » mais indicateurs affichés dès 2 s. Clarification : le seuil MIN=2 s est le délai d'AFFICHAGE de la toile (attend que sonie/acuité 250 ms soient stables) ; la fluctuation utilise tout le tampon PSY_BUFSEC=5 s → fenêtre GLISSANTE de 5 s, nulle sous ~1,2 s (psyFluct), provisoire entre 2 et 5 s (tampon en remplissage), pleine à 5 s. Valable pour ~1–8 Hz dès 2 s ; modulations < ~0,5 Hz seulement à 5 s. Aide corrigée : FR (liste modèles : « fenêtre glissante de 5 s, provisoire avant 5 s ») + récap glossaire trilingue (glissante/pleine après 5 s). node --check + loadtest OK. RESTE : sonie temps-variable ; promotion beta→prod.

- **2.2.17-beta** : **Fenêtres de calcul documentées**. La section Psychoacoustique (FR) indiquait déjà les fenêtres (sonie/acuité 250 ms, rugosité ~170 ms recalc ~1 s, fluctuation 5 s) mais la tonalité n'en avait pas → ajoutée (Aures 2 s ; TNR/PR recalc ~1 s sur le spectre fin FFT). Comme EN/ES n'ont pas la liste détaillée des modèles, ajout d'un **récap « Fenêtres de calcul »** dans le glossaire (trilingue FR/EN/ES) : sonie/acuité 250 ms · rugosité ~170 ms (~1×/s) · tonalité 2 s (TNR/PR ~1 s) · fluctuation 5 s. Valeurs cohérentes avec PSY_WIN={N:0.25,S:0.25,R:1.0,T:2.0,F:5.0} et la fenêtre D&W 8192 éch. node --check + loadtest OK. RESTE : sonie temps-variable ; promotion beta→prod.

- **2.2.16-beta** : **Glossaire = onglet dédié, trilingue**. Le glossaire devient un onglet de premier niveau dans la barre helpnav (« AIDE & GUIDE »), à côté de Prise en main / Mesure & indices / Analyse spectre / Réglages & export. Ajout d'un 5e groupe dans HELP_GROUPS (Glossaire/Glossary/Glosario). groupHelp repère la section par une **classe `gloss`** (robuste malgré la désync d'index entre langues : FR 18 h3, EN 17, ES 15) → groupe 4 quel que soit l'index/langue. Glossaire **traduit FR/EN/ES** (grandeurs acoustiques + psychoacoustiques, formules identiques sub/sup, ajouté à HELP_FR via le DOM et aux templates HELP_EN/HELP_ES avant leur helpfoot). node --check OK, loadtest OK, regroupement vérifié (gloss→4 en FR/EN/ES). RESTE : sonie temps-variable ; promotion beta→prod.

- **2.2.15-beta** : **Glossaire dans l'aide (FR)**. Nouvelle section « Glossaire — grandeurs & critères » : grandeurs acoustiques (dB(A)/dB(C), LAF/LAS, Leq avec formule, LCpeak, émergence, Bark) et psychoacoustiques (Sonie N + lien sone↔phon N=2^((L−40)/10), N5, Acuité S formule barycentre DIN 45692, Rugosité R formule D&W, Fluctuation F formule Fastl, Tonalité Aures, TNR=Lt−Ln, PR=Lm−10log10(½(10^Ll/10+10^Lu/10)), PA=N5(1+√(wS²+wFR²))) — chacune avec unité, norme, référence et formule simplifiée (sub/sup HTML). NB : aide trilingue FR/EN/ES ; glossaire ajouté au FR uniquement pour l'instant (EN/ES à porter). loadtest OK ; JS check OK. RESTE : porter glossaire EN/ES ; sonie temps-variable ; promotion beta→prod.

- **2.2.14-beta** : **Note sur les limites des indicateurs** (rapport PDF + CSV + aide). Vérifié dans le code : la **correction fréquentielle du micro** (compGain, si profil actif) EST appliquée à sonie/acuité/fluctuation/tonalité/TNR/PR (chemin spectral : curFin→binPowD et bpZ→curBndZ, correction avant pondération) ; la **rugosité D&W** est calculée sur le tap temporel brut (offset seul, correction fréquentielle non appliquée — mais robuste car basée sur la profondeur de modulation). Notes ajoutées : (1) rapport psyReportBlock (bloc gris détaillé) ; (2) CSV psyCsvBlock (3 lignes NOTE) ; (3) aide (paragraphe Réserves étoffé). Contenu : grandeurs indicatives non certifiées, micro non métrologique, application de la correction par indicateur, sonie/acuité sensibles à calibration+réponse micro vs rugosité/fluctuation/tonalité robustes (rapports), sonie stationnaire (N5 approché). test_psycho 18/18, test_tnr 8/8, test_roughdw 10/10, loadtest OK.

- **2.2.13-beta** : **Bornes de toile réalistes + cohérence sone/phon**. (1) Les bornes par défaut {N:100,S:5,R:5,F:3,T:2} avaient été gonflées à l'époque de l'ancienne rugosité rapide (qui montait à ~10 asper) → avec le vrai D&W (0–~2 asper) elles écrasaient le polygone au centre (cause réelle du « tout au centre »). Nouvelles bornes **{N:40,S:4,R:2,F:2,T:1.5}** (réf. 1 asper/1 vacil ~ mi-échelle ; N 40 sone ≈ 90 phon). Migration : les anciens défauts {40,4,1,1,1} ET {100,5,5,3,2} sont recalés vers les nouveaux (donc les prefs persistées de l'utilisateur se mettent à jour). Reset + fallback + aide MAJ. (2) L'encart « SON » dérivait le phon de N5 (incohérent avec l'axe Sonie en cur) → psyDescribe utilise maintenant la sonie AFFICHÉE (cur, ou mean si MOY) et montre **« X sone ≈ Y phon »** (loi de Zwicker ×2 sone=+10 phon). Ex. 2,2 sone ≈ 51 phon. test_psycho 18/18, test_tnr 8/8, test_roughdw 10/10, loadtest OK. RESTE : sonie temps-variable ; promotion beta→prod.

- **2.2.12-beta** : **Correctif latence + encart gêne repensé**. (1) LAG : le D&W live (~80 ms Node → 150-350 ms iPhone) tournait sur le thread principal toutes les 1 s = hitch visible. Déplacé dans un **Web Worker** (psyRoughWorkerInit construit la source du worker à partir de fft.toString()+rdw*.toString()+tables JSON.stringify ; psyRoughPost lit le tap sur le thread principal puis poste au worker ; onmessage → PSY._Rcur). Calcul hors thread → zéro hitch. Repli synchrone si Worker indispo (jsdom/anciens navigateurs). Worker validé fonctionnellement (AM70→1.003, AM30→0.396). TNR/PR throttlé 0,5→1 s. (2) ENCART : la description couvrait la toile → passée derrière un **bouton « SON »** (à côté de MOY, #psyDescBtn, état S.psyDesc, défaut OFF). Quand OFF : petite ligne d'aide ; quand ON : encart gêne PA + description. Aide MAJ. test_psycho 18/18, test_tnr 8/8, test_roughdw 10/10, emulate OK, loadtest OK. RESTE : sonie temps-variable ; promotion beta→prod (racine en 2.0.89) ; durcir deploy.py (retries/fallback Contents).

- **2.2.11-beta** : **Encart gêne + description du son** sur la toile psycho. La gêne PA au centre étant peu lisible quand le polygone est ramassé, un encart permanent s'affiche en bas (quand aucun axe n'est sélectionné, à la place de l'ancienne ligne d'aide) : « Gêne PA <val> · <qualificatif> » (vert/orange/rouge) + description auto du son via `psyDescribe()` (niveau en ~phon depuis N5, timbre aigu/sourd via acuité, tonal via TNR≥8/T≥1 avec fréquence de la raie dominante, rugueux via R, fluctuant via F). Ex. « Son fort (~78 phon), tonal (100 Hz), rugueux ». Sélectionner un axe rebascule sur le readout détaillé. Aide MAJ. node --check + loadtest OK. RESTE : sonie temps-variable ; Web Worker rugosité.

- **2.2.10-beta** : **Validation par balayage fréquentiel + aide psycho réécrite**. emulate.js étendu : balayage 63 Hz→8 kHz (tons purs 60 dB) et sonie×niveau (40/60/80 dB à 125/1k/4k) confrontés à MOSQITO (sweep_ref.json via loudness_zwst/sharpness_din_st). Résultat : **sonie 8/8 et acuité 8/8** à ±2 % de MOSQITO sur tout le spectre, à travers le banc de filtres IIR réel (ferme le trou « validé à 1 kHz seulement »). Sonie×niveau ~2 % partout. **Aide psycho entièrement réécrite** : intro toile/PA, liste claire des 5 modèles (ISO 532-1 / DIN 45692 / D&W complet / Fastl / Aures + TNR/PR ECMA-74) avec fenêtres, mention « toujours en Z », section Validation (déf. normatives + MOSQITO ~2 % de 63 Hz à 8 kHz, 1 asper, 1 vacil, TNR/PR identiques) et réserves. Rapport validation_psycho.md régénéré avec le balayage. sweep_ref.json ajouté au dépôt. test_psycho 18/18, test_tnr 8/8, test_roughdw 10/10, loadtest OK. RESTE : sonie temps-variable ; Web Worker rugosité ; signaux WAV réels.

- **2.2.9-beta** : **CORRECTIF MAJEUR — psychoacoustique toujours en Z (non pondéré)**. Bug détecté via le banc d'émulation : `S.curBnd` (puissances de bande) recevait la pondération d'affichage A/C (`wSel[i]` en mode FFT, `wfc[b]` en mode IIR) AVANT d'alimenter psyLoudness → sonie/acuité/fluctuation calculées sur un spectre pondéré A quand l'utilisateur était en pondération A (cas par défaut !) = valeurs biaisées (ex. 125 Hz sous-évalué de ~16 dB). Correctif : nouveau tableau `S.curBndZ` (bandes NON pondérées, correction micro conservée mais sans A/C) rempli dans les deux chemins (FFT+IIR, ×K Parseval), lu par psyLoudness (`srcBnd=S.curBndZ||S.curBnd`). `binPowD` (spectre fin → tonalité/TNR) était déjà non pondéré, donc tonalité/TNR/rugosité non affectés. **Vérifié : sonie identique en Z/A/C** (1 kHz→4.17, 125 Hz→2.51, 4 kHz→6.21 ; écart A−Z = 0.000). Aide MAJ. emulate 5/5 réf OK, test_psycho 18/18, test_tnr 8/8, test_roughdw 10/10, loadtest OK. RESTE : sonie temps-variable ; Web Worker rugosité ; balayage fréquentiel sonie/acuité via banc IIR ; signaux WAV réels.

- **2.2.8-beta** : **Banc d'ÉMULATION end-to-end (emulate.js) + recalage fluctuation**. emulate.js extrait les VRAIES fonctions DSP de index.html (fft, banc IIR IEC 61260, ISO 532-1, DIN 45692, D&W, Fastl, Aures, TNR/PR) dans un bac à sable Node avec un S simulé calibré (offset=−20log10(2e-5)=93.98 → signaux générés en Pa), reproduit le flux réel (banc IIR→curBnd, spectre Parseval→binPowD) et valide les définitions normatives. Résultats : **Sonie 1 kHz 40/50/60/70/80 dB → 1.03/2.07/4.17/8.54/17.39 sone** (déf. 1 sone@40 dB, ×2/+10 dB ✓ ; MOSQITO 1.01/4.09/17.10, ~2 %) ; **Acuité 1 kHz 60 dB → 1.01 acum** (déf. ✓ ; MOSQITO 1.01 ; 3 kHz→2.15 vs 2.14) ; **Rugosité AM 70 Hz → 1.00 asper** (✓) ; **Fluctuation AM 4 Hz → 1.00 vacil** après recalage **PSY_CAL_F 1.44→1.68** (pic bien à ~4 Hz) ; **bruit rose 50 dB → rugosité 0.016 asper** (correctif 2.2.6 validé end-to-end). test_psycho: plage fluctuation synthétique élargie à [0.9,1.4] (le modèle synthétique du banc lit 1.167 vs pipeline complet 1.003). NB : T-PR d'un ton pur DANS LE SILENCE sature (~100 dB, cas dégénéré sans bruit ; correct dès qu'il y a un plancher). test_psycho 18/18, test_tnr 8/8, test_roughdw 10/10, loadtest OK. RESTE : sonie temps-variable ; Web Worker rugosité ; ECMA-418-2.

- **2.2.7-beta** : **Export psychoacoustique dans le CSV et le rapport PDF**. Helper commun `psyStats()` (lit PSY.cur/mean/p95 {N,S,R,F,T}, PA, N5, tnrT/prT/tones ; renvoie null si aucune mesure). `psyCsvBlock()` → section « INDICATEURS PSYCHOACOUSTIQUES » (5 indices × actuel/moyenne/P95 + PA + N5) + « TONALITE NORMATIVE (ECMA-74) » (T-TNR/T-PR + raies proéminentes), insérée dans buildCsv avant l'historique. `psyReportBlock()` → section rapport HTML (table .rep-psy + cartes rep-grid PA/N5/T-TNR/T-PR + liste des raies + note sur les modèles/MOSQITO), insérée dans buildReportHTML avant le commentaire ; CSS .rep-psy ajouté à REPORT_CSS. psyUpdate tourne pendant toute la mesure (pas seulement sur l'écran Psycho) → stats dispo pour tout export. Rugosité = D&W (depuis 2.2.6). Décimales : sonie 1, autres 2. Se tait proprement si PSY._cnt=0. test_psycho 18/18, test_tnr 8/8, test_roughdw 10/10, loadtest OK. RESTE : sonie temps-variable (loudness_zwtv) ; Web Worker pour la rugosité (éviter le hitch ~1 s) ; ECMA-418-2.

- **2.2.6-beta** : **CORRECTIF rugosité live**. Le modèle multi-bandes rapide sur-évaluait massivement le bruit large bande (bruit rose/blanc 52 dB → ~10 asper, P95 33.9 !) car il interprète les fluctuations d'enveloppe incohérentes du bruit comme de la rugosité (plancher de cohérence 0,5 insuffisant ; il corrèle des spectres de modulation, pas les enveloppes temporelles). Diagnostic Node : fast=9.8/10.3 asper vs D&W=0.017/0.003 sur pink/white 52 dB. **La rugosité live utilise désormais le D&W complet validé** (psyRoughDWval : tap psyAn → 8192 pts ~170 ms, throttle ~1 s dans psyUpdate ; remplace psyRoughness rapide). **Calibration corrigée** : le D&W a besoin du SPL réel — le tap PCM est mis à l'échelle Pa via k=2e-5·10^(offset/20) avant calcul (sans ça, niveaux sous le seuil d'audition → 0 composante audible → D&W=0.00, bug observé sur bruit de fond). Tap sur l'axe Rugosité = recalcul immédiat (psyRoughDWnow). PSY_CAL_R/psyRoughMB conservés mais hors du chemin live. Coût ~50 ms Node (~100-250 ms iPhone) une fois/s → léger hitch possible ; piste future = Web Worker. test_psycho 18/18, test_tnr 8/8, test_roughdw 10/10, loadtest OK. RESTE : export psycho (3 stats + TNR/PR + D&W) dans CSV + rapport PDF (EN COURS).

- **2.2.5-beta** : **Rugosité Daniel & Weber COMPLÈTE (D&W 1997, port fidèle MOSQITO roughness_dw — VALIDÉ quasi-identique)**. Bloc `psyRoughDW(pcm,fs)` + helpers/tables préfixés `rdw*` : RDW_BF/BZ (freq↔bark), RDW_EX/EY (transfert oreille Zwicker), RDW_GX/GY (pondération gzi Aures), RDW_LX/LY (LTQ « roughness » ½-bark), RDW_HC (points de contrôle H2/H5/H16/H21/H42), rdwBuildH (matrice 47×n cachée), rdwIFFT (via fft app : conj→fft→conj→/n), rdwInterp. 3 étages exacts : (1) excitation spécifique 47 canaux ½-Bark (pentes Terhardt s1=−27, s2 dépendant du niveau, reconstruction spectrale + IFFT), (2) profondeur de modulation via Hi(f_mod) + h0, (3) rugosité spécifique avec corrélation croisée ki(i,i+2) et R=0.25·Σ. **Coûteux (~70 ms/trame) → calcul À LA DEMANDE** : tap sur l'axe **Rugosité** → `psyRoughDWnow()` (tap psyAn 16384 → fenêtre 8192 radix-2) → PSY.rdw, affiché « Daniel & Weber (précis) : X asper » sous le readout. Pas de calibration (modèle calé nativement sur 1 asper). **Banc test_roughdw.js 10/10** (±0,12 asper vs rough_ref.json ; dépendance au niveau capturée 40/60/80 dB→0.54/1.00/1.40, pic 70 Hz, décroissance f_m, profondeur, porteuse). Le proto Node (proto_roughdw.js, FFT Bluestein taille quelconque) matche roughness_dw à ±0,002 asper sur 11 cas en fenêtre 9600 native. La toile live garde le modèle multi-bandes rapide (PSY_CAL_R). test_psycho 18/18, test_tnr 8/8, loadtest OK. Outils : proto_roughdw.js, rough_ref.json, ltq_rough.json, gen (dans le transcript). RESTE : export 3 stats + TNR/PR + D&W dans rapport/CSV ; sonie temps-variable ; ECMA-418-2.

- **2.2.4-beta** : **Tonalité normative TNR/PR (ECMA-74) — port fidèle MOSQITO, VALIDÉ IDENTIQUE**. Bloc `psyTnrPr(specDb,freq)` + helpers préfixés `tnr*` (tnrLTH seuil audition D.7.1, tnrCB bande critique D.8, tnrLowCB/tnrUpCB D.10, tnrNthOct 1/24-oct base-10, tnrSmooth lissage 1/24-oct énergétique, tnrPeakLvl niveau de raie intégré, tnrScreen screening Bray/Sottek [max local + >lissé+6 dB + >LTH+10 dB + largeur<bande critique], tnrFindHi plus hautes raies par bande critique, psyTnrMain, psyPrMain). TNR=Lt−Ln (raie vs bruit bande critique) ; PR=Lm−moy(Ll,Lu) (bande vs voisines) ; proéminence dépendante de f ; agrégation T-TNR/T-PR sur raies proéminentes. **Indépendant de l'offset/fenêtre (différences en dB)**. Alimenté par S.binPowD+offset (throttle 0,5 s dans psyUpdate) → PSY.tnrT/prT/tones. Clic axe **Tonalité** affiche TNR/PR + raie dominante. Bug corrigé au portage : tnrLowCB borne haute = borne BASSE de la bande centrale (Python `f2,_=_critical_band`). **Banc test_tnr.js 8/8** (T-TNR/T-PR identiques MOSQITO à ±0,1 dB sur 1k/1k+3k/200Hz/bruit). test_psycho 18/18, loadtest OK. Aure (t.u.) conservé pour l'axe. Outils: gen_tnr.py, proto_tnr.js, tnr_ref.json. PROCHAIN : #1 rugosité D&W complète (filtres+Hilbert+dépendance niveau). RESTE aussi : export 3 stats + TNR/PR dans rapport/CSV.

- **2.2.3-beta** : **Sonie = ISO 532-1 TABULÉE (port fidèle de MOSQITO / DIN 45631)**, remplace le Zwicker analytique. Tables `ISO_RAP/DLL/LTQ/A0/DDF/DCB/ZUP/RNS/USL` + `ISO_F28`. `isoMainLoudness(spec28,field)` (correction basses freq table A.3, LCB 3 premieres bandes critiques, a0 oreille, seuil LTQ, sonie de coeur) -> nm[21] ; `isoCalcSlopes(nm)` (pentes superieures tabulees USL dependantes niveau+bande, integration 0,1 Bark) -> {N, Ns(240)} ; `isoRnsIndex`. `psyLoudness` bati les 28 tiers d'octave 25 Hz-12,5 kHz depuis S.curBnd/offset via table `ISO_MAP` (nearest log2, MAJ si S.bands change), rebin 240->24 Bark pour Nspec (acuite/fluctuation). **Validation : coeur IDENTIQUE a MOSQITO a la precision machine** (N=4.090 vs 4.09 ; DNspec ~1e-14 sur vecteurs tiers d'octave identiques ; bruit rose 10.8=10.8). Residu tons purs (-10%) = front-end tiers d'octave (bins FFT vs jupes filtres MOSQITO ~40 dB bandes voisines), PAS l'algo. **PSY_CAL_F recale 1.384->1.44** (echelle sonie changee). Confrontation MOSQITO complete (loudness_zwst/sharpness_din/roughness_dw) : rapport comparaison_mosqito.html regenere. Banc test_psycho **18/18** (bornes sonie ISO 0.93/1.79/3.49/6.94/13.97). node --check + loadtest OK. Outils: gen_mosqito.py, iso_ref.py, proto_iso.js, compare_mosqito.js, build_cmp_report.py. RESTE : (option) jupes tiers d'octave type MOSQITO ; rugosite D&W complet (filtres+Hilbert, dependance niveau) ; tonalite TNR/PR si besoin normatif.

- **2.2.2-beta** : **modeles psycho MULTI-BANDES** (remplacent mono-bande/proxy). **Rugosite** = Daniel & Weber : analyseur DEDIE `S.psyAn` (fftSize **16384** fixe, independant de fftN, ~340 ms) + `S.psyTap`, cable via `psyTapWire(src)` (acquireMic, replayTeardown, replay bufferSource). `psyRoughMB(pcm,fs)` : FFT Hann, **canaux auditifs CHEVAUCHANTS** 0,5 Bark (demi-largeur `PSY_RHW=1` Bark, triangulaire), enveloppe par canal via **autocorrelation spectrale** C[d]=Sum X[k]X*[k-d] (d=20..300 Hz), indice modul. m=(2*sqrt(acPow)/dc)^`PSY_RP=1.8`, seuil energie canal dc<dcmax*1e-3 (ignore fuite), **coherence inter-bandes** coh=0.5+0.5*max(corr voisins), R=`PSY_CAL_R=0.345`*Sum(m*coh)*0.5. `psyBarkF(f)` continue. **Fluctuation** = Fastl band-wise : `psyFluctBW(nhist,dt)` sur N'(z,t) par bande (buffer stocke `Ns`=Nspec par echantillon), moyenne de profondeur (psyModBandP 0.2-8 Hz WF) **ponderee sonie** -> **independante du niveau**, `PSY_CAL_F=1.384` (recale sur VRAI pipeline sonie, compression E^0.23). **Tonalite** = Aures : `psyTonalAures(binPow,df)` pics tonaux (emergence>6 dB sur plancher median ~50 Hz), poids w1(f) pic ~700 Hz * (1-exp(-dL/15)), sqrt(somme quad), `PSY_CAL_T=1.98`. psyUpdate : R throttle 120 ms, **T throttle 150 ms** (`PSY._tT/_Tcur`, remplace emergence/15), stockage `Ns`, F via band-wise. Bench **test_psycho.js REECRIT 18/18** (rugosite pic 50-70/pur~0/50%=0.59/indep niveau, fluct pic 4 Hz/lineaire/indep niveau, tonalite pur 1kHz=1.0/bruit~0/1kHz>3kHz). node --check + loadtest OK. Aide + commentaires MAJ (retrait mentions mono-bande/proxy). Prototypes Node /tmp/proto*.js + /tmp/cal.js. RESTE : confrontation MOSQITO, export 3 stats (actuel/moy/P95) dans rapport+CSV, regenerer rapports validation.

- **2.2.1-beta** : **refonte du moteur psycho** selon spec Thibaut. Tampon circulaire **5 s** (PSY.buf) echantillonne ~20 Hz (indispensable pour la fluctuation 4 Hz). **Recalcul des 5 indicateurs toutes les ~150 ms** (PSY._recalcT), chacun sur SA fenetre : Sonie 250 ms, Acuite 250 ms, Rugosite 1 s, Tonalite 2 s, Fluctuation 5 s (PSY_WIN). Pour chaque indicateur, 3 stats : **actuelle** (PSY.cur), **moyenne depuis le debut** (PSY.mean), **P95 peak-hold** (PSY.p95, via PSY._hist + psyPctile, cap 40000+decimation). drawPsycho : polygone plein = actuel (MOY = moyenne), **tic ambre = P95** (remplace max-hold), lecture d'axe = actuel/moy/P95 + fenetre + %. Affichage apres 2 s. **Reglage 'Fenetre psycho 2/4/8 s' SUPPRIME** (ligne menu + psyWinPop + handlers + setPsyWin) : fenetres desormais fixees par definition. PSY.N/S/R/F/T conserves en miroir de cur (compat rapport/CSV). Bouton MOY -> PSY._cnt. test_psycho 11/11, loadtest OK.

- **2.2.0-beta** : refonte lecture toile psycho. (1) **Attente 2 s** avant tout affichage d'indicateur (ready = span>=2 s ou MOY dispo) ; avant : anneau de progression 'mesure en cours X/2.0 s' au centre, grille + noms d'axes seulement. (2) **Instant rafraichi toutes les 0,5 s** (PSY._instT) ; N/S moyennes sur ~0,5 s (fenetre court-terme optimale), R bloc ~200 ms, F fenetre ~2 s, T courant. Compute interne reste a 20 Hz (moyennes, R ~8 Hz, MOY). (3) **Max-hold par indicateur** (PSY.max, toutes trames) : tic ambre #FFB454 perpendiculaire sur chaque axe = crete atteinte, comme le peak-hold des tiers d'octave ; ajoute aussi dans la lecture d'axe (reel + normalise % + max). (4) Valeurs R/F/T stockees REELLES (non ecretees ; ecretage uniquement au trace v/borne). psyReset remet PSY.max/_instT. Aide MAJ. test_psycho OK.

- **2.1.9-beta** : nouveaux **defauts**. Bornes de normalisation psycho : Sonie **0-100 sone**, Acuite **0-5 acum**, Rugosite **0-5 asper**, Fluctuation **0-3 vacil**, Tonalite **0-2 t.u.** (PSY_BOUNDS + etat psyBnd + fallback + bouton reset renomme 'Valeurs par defaut'). Plages des steppers elargies (N 10-200, R 0.5-10, F 0.5-6, T 0.5-4, pas 0.5). **Fenetre FFT par defaut = 4096 pt** (fftN 16384->4096 ; ~11.7 Hz de resolution a 48 kHz, plus reactif en temps). **Migration unique** (flag localStorage sono_mig219) : si psyBnd == ancien defaut {40,4,1,1,1} -> nouveau ; si fftN==16384 -> 4096. Aide MAJ. test_psycho OK (valeurs absolues inchangees).

- **2.1.8-beta** : **BUG appui long ECRAN corrige**. Le sous-menu de choix de vue (screenPop) s'ouvrait mais selectionner une vue ne faisait rien : ligne ~4633 `document.querySelectorAll('.ppopt')` (handler des presets de spectre) etait GLOBAL et ecrasait les onclick de screenPop (attaches avant) par applyPreset(data-preset=null). Corrige en `$('presetPop').querySelectorAll('.ppopt')`. psyWinPop n'etait pas touche (attache apres). Verifie en jsdom : clic 'psy' -> panePsy, 'sgram' -> paneSgram.

- **2.1.7-beta** : pendant l'INIT MICRO (prechauffage), **plus aucune valeur calculee ni affichee**. Le bloc warm de loop() appelle desormais `blankInit()` au lieu de updateNumbers(L)+drawActive() : readouts en tirets (--.-), canvas actif efface avec 'INIT MICRO...' centre, et surtout **drawActive n'est pas appele -> psyUpdate ne tourne pas** (la toile n'accumule plus le transitoire d'amorçage iOS). Garde supplementaire : psyUpdate jamais si S.warmT>0. Leq deja non integre en init ; spectrogramme/historique deja vierges. Filtres IIR continuent de se stabiliser en arriere-plan (lvlDet), mais rien n'est montre.

- **2.1.6-beta** : (1) **Fenetre psycho** deplacee de l'onglet Affichage vers **Signal** (mPsyWin data-cat=signal). (2) **Bornes des indicateurs reglables** dans le menu ECHELLES (kDyn) sur l'ecran Psycho : nouvelle section dpSecPsy (5 steppers N/S/R/F/T avec unites + bouton 'Valeurs litterature') ; etat S.psyBnd (persiste), utilise par drawPsycho a la place de PSY_BOUNDS (defaut). renderDynPop gere isPsy (titre ECHELLES · PSYCHO). (3) **Bouton MOY** sur le pane Psycho (psyAvgBtn, vert comme le spectre) : S.psyAvg -> la toile affiche la **moyenne sur toute la mesure** (accumulateur PSY.sum/PSY.cnt) au lieu de la moyenne glissante ; reset au resetMeasure, .dis tant que PSY.cnt=0. Tous valides (test_psycho OK, loadtest OK).

- **2.1.5-beta** : (1) **Appui long sur ÉCRAN** -> popup **screenPop** (choix direct de la vue : Spectre / Spectrogramme / Évolution / Valeurs / Psycho) ; tap = cycle inchange. goScreen() factorise. Garde S._longScreen pour ne pas declencher le click apres l'appui long. (2) Psycho sur la duree selectionnee : ajout PSY.span (couverture reelle du tampon) + **indicateur de stabilisation** sur le radar (texte 'stabilisation X.X/N s' + arc de progression orange) tant que span<fenetre -> montre que la moyenne glissante se cale sur 2/4/8 s. (3) **Fluctuation bridee** tant que span<1.2 s (pas fiable pour 4 Hz au demarrage). Le titre affiche deja la fenetre choisie.

- **2.1.4-beta** : corrections toile psycho. (1) **BUG** : panePsy etait absent de la liste de masquage des panes (L4527) -> le radar restait affiche par-dessus les autres ecrans / a l'ouverture de l'aide. Corrige (panePsy ajoute). (2) **Fenetre psycho** : tap = cycle 2/4/8 s ; **appui long (500 ms) = popup de choix** (psyWinPop, options 2/4/8 s + note). Plus de psyReset au changement (la fenetre glissante se rallonge/raccourcit a la volee, plus de remise a zero visible). (3) Confirme : computeSpectrum (FFT + tiers d'octave + niveaux/Leq) tourne a CHAQUE trame quel que soit l'ecran, psyUpdate s'alimente dessus -> tous les calculs en parallele en continu. La toile est bien une moyenne glissante sur la fenetre (polygone plein) + instantane (pointille).

- **2.1.3-beta** : (1) Bornes de normalisation de la toile calees sur la LITTERATURE : rugosite et fluctuation **1.0** (au lieu de 1.5 ; valeurs typiquement < 1 asper/vacil, ref. product sound quality), sonie 40 sone, acuite 4 acum, tonalite 1. (2) **Clic sur un indicateur** : pointerdown sur psyCv -> selection de l'axe le plus proche (centre = deselection) ; l'axe/sommet est mis en surbrillance et un bandeau affiche la valeur **reelle** (unite) + **normalisee** (% et borne). PSY.sel, PSY._geom, PSY._wired. Aide MAJ (bornes + astuce clic).

- **2.1.2-beta** : PSYCHOACOUSTIQUE Phase 3 — SONIE avec ETALEMENT DE MASQUAGE (modele d'excitation ISO 532-1). psyLoudness reecrit : intensite par bande de Bark -> motif d'excitation par etalement (pente sup. dependante du niveau Su=24+230/f-0.15*L dB/Bark, pente inf. 27 dB/Bark) -> sonie specifique de Zwicker integree. Nouvelles constantes PSY_BARK_FC (24 freq centrales Bark) + PSY_CN=0.1253 (calage 1 sone @1kHz/40dB). Corrige la sous-estimation haut niveau : loi sonie/niveau = 1.00/2.16/4.33/8.45/16.20 sone @40/50/60/70/80 dB (cible doublement/10 dB 1/2/4/8/16). Acuite recalee OK (0.95 acum @1kHz, croit avec la freq : 2.43 @4kHz). Banc test_psycho.js etendu (11 controles OK, ajout loi de niveau 50/70/80 dB). Rapport global SONO-VAL-000 regenere (15 p.) : Partie III MAJ (methode excitation+masquage, tableau loi de niveau, courbe sonie superposee au repere, caveat haut-niveau retire). P3 restant : correlation inter-bandes rugosite + confrontation jeux MOSQITO reels.

- **2.1.1-beta (suite)** : PROCEDURE DE VALIDATION GLOBALE (make_global_report.py) + RAPPORT FINAL (SONO-VAL-000) etendus a la PSYCHOACOUSTIQUE = **Partie III (SONO-VAL-PSY-001)**. La procedure lance desormais aussi test_psycho.js, psycho_export.js et psycho_figs.py, et insere la Partie III (methodes, durees mini, tableau de verification 8 controles, 5 courbes) dans le .docx ET le PDF CETIM. Rapport global = 14 pages, 3 parties, tous bancs OK. Scripts psycho normalises cwd=tests/ (chemins relatifs, fallback ../index.html) et versionnes via deploy.py. Rapport psycho autonome conserve : Rapport_validation_psychoacoustique_CETIM.pdf.

- **2.1.1-beta** : (1) Reglage **Fenetre psycho** (onglet Affichage, mPsyWin) : **2 / 4 / 8 s** (defaut 4 ; mini 2 s car la fluctuation a 4 Hz exige >=2 s ; rugosite ~200 ms suffit). psyReset au changement, persiste (psyWin). (2) **Rapport de validation psychoacoustique** (ref. SONO-VAL-PSY-001) : pipeline tests/psycho_export.js (balayages -> psycho_data.json) + tests/psycho_figs.py (5 courbes CETIM : sonie vs niveau, acuite vs freq, rugosite vs f-mod, rugosite vs profondeur, fluctuation vs f-mod) + tests/psycho_report.py (HTML CETIM -> PDF wkhtmltopdf, 3 pages) -> tests/Rapport_validation_psychoacoustique_CETIM.pdf. Calibration calib_psy.js. test_psycho.js (8 controles) OK. Bilan : sonie 1.01 sone@40dB / 3.28@60dB, acuite 0.93 acum, rugosite pic 70 Hz + lineaire, fluctuation pic 4 Hz — tous conformes. Limite documentee : sonie sous-estime en haut niveau (pas d etalement de masquage), a affiner P3.

- **2.1.0-beta** : PSYCHOACOUSTIQUE Phase 2 — vrais modeles rugosite/fluctuation (modulation). Rugosite = Daniel & Weber : enveloppe du signal (getFloatTimeDomainData sur S.tap), redressement + passe-bas ~400 Hz, decimation ~2 kHz, spectre de modulation Goertzel pondere PSY_WR (pic 70 Hz), sommation QUADRATIQUE, cale PSY_CAL_R=1.0113 (1 kHz/100% AM @70 Hz -> 1 asper) ; calcul sur bloc ~200-340 ms, throttle ~8 Hz (PSY._Rcur). Fluctuation = Fastl/Osses : modulation de N(t) sur la fenetre (~2 s mini pour 4 Hz), pondération PSY_WF (pic 4 Hz), PSY_CAL_F=1.0. Fonctions psyModBandP/psyRoughness/psyFluct ; proxies flux/ecart-type SUPPRIMES. Durees mini (notices) : rugosite ~200 ms, fluctuation >=2 s (contrainte 4 Hz) -> fenetre 4 s gardee. Banc test_psycho.js (8 controles OK : sonie 1.01 sone, acuite 0.94 acum, rugosite pic 70 Hz + lineaire en profondeur, fluctuation pic 4 Hz). Aide MAJ. Calibration via calib_psy.js. P3 = decomposition par bande critique + validation MOSQITO.

- **2.0.99-beta** : NOUVEL ECRAN **PSYCHOACOUSTIQUE** (Phase 1). Ecran 'Psycho' (cycle ECRAN apres Valeurs, pane panePsy/psyCv) affichant une TOILE d'araignee de 5 indicateurs normalises sur fenetre glissante (S.psyWin=4 s) : Sonie N (sone, Zwicker simplifie depuis les tiers d'octave, sonie specifique par Bark + seuil en champ, cale a 1 sone @1kHz/40dB via constante 0.17), Acuite S (acum, ponderation DIN 45692 sur N'(z)), Rugosite R (asper, ESTIMATION flux spectral), Fluctuation F (vacil, ESTIMATION ecart-type de N), Tonalite T (emergence S.psyEmg/15). Centre = gene PA (Zwicker: N5,S,R,F ; vert<15/orange/rouge>40). Polygone plein=moyenne fenetre, pointille=instantane. Module psyLoudness/psySharpness/psyUpdate(~20Hz)/drawPsycho ; psyReset au resetMeasure ; emergence captee L2684 (S.psyEmg). Aide + HELP_GROUP_OF etendu (section 17, groupe 1). Bornes PSY_BOUNDS {N40,S4,R1.5,F1.5,T1}. P2 = R/F modeles complets (Daniel&Weber/Osses) + validation P3.

- **2.0.98-beta** : rétroéclairage revu. (1) Halo **permanent** (pas seulement a l'appui) : glow de base des touches renforce (drop-shadow 9px, alpha .72) + textes/LED, renforce encore a l'appui. (2) **Couleur du halo liee au theme** via variable `--glowRGB` definie par theme (marine 79,195,255 · acier 150,190,235 · clair 60,120,210 · oled 100,190,255 · neon 255,90,110 · gameboy 150,168,70 · matrix 80,255,140) ; le halo utilise rgba(var(--glowRGB),a). Suppression des surcharges par theme codees en dur.

- **2.0.97-beta** : mode **Rétroéclairage** (onglet Affichage, `mBacklight`, On/Off). Classe `body.backlit` : halo lumineux (filter drop-shadow + brightness) sur l'ecran (.lcd), la valeur (.num), le badge, et les touches (.key) avec glow des libelles (.kf/.kv) et de la LED (.dot) ; renforce a l'appui. Teinte du halo suivant le theme (neon rouge, matrix vert, gameboy olive ; bleu par defaut). Independant du theme, persiste (backlight), applique dans applyTheme + au toggle. i18n 'Retroeclairage'.

- **2.0.96-beta** : gain d'ecoute Auto plus FORT. Live : makeup **+30 dB** (au lieu de +18) + limiteur. Relecture : normalisation sur le **RMS** (vise ~ -15 dBFS RMS, borne 0.2..64x) au lieu du pic -> bien plus fort a l'oreille, le limiteur (-1 dB, ratio 20) tenant les cretes. Sous-echantillonnage du buffer pour le calcul RMS sur gros enregistrements.

- **2.0.95-beta** : GAIN D'ECOUTE AUTOMATIQUE (♪). Nouvelle option **Auto** dans 'Gain ecoute' (lsnGainSel). `lsnGainLinear()` : si **relecture** -> normalise d'apres le PIC de l'enregistrement (REPL.buf, vise ~ -1 dBFS, borne 0.1..64) = meilleur gain sans ecreter ; si **live** -> makeup +18 dB avec **limiteur** (DynamicsCompressor threshold -1 dB, ratio 20, attack 3 ms, release 250 ms) insere en fin de chaine d'ecoute -> augmente le gain SANS saturer. lsnBuild ajoute le limiteur en mode auto ; lsnOff le deconnecte ; bascule auto<->fixe via lsnRebuild ; recalcul du gain au chargement d'un replay. Etat lsnAuto persiste. Metrologie inchangee (tests OK).

- **2.0.94-beta** : (1) Bouton calibration renomme **'✓ Terminé'** -> retour DIRECT a l'ecran de mesure precedent (S.prevScreen) au lieu du menu (etait redondant avec la touche MENU). (2) **Frequence d'echantillonnage reglable** (onglet Signal, `mSampleRate`) : **Auto (defaut appareil) / 24 / 12 / 8 kHz**. `makeCtx()` cree l'AudioContext avec {sampleRate:S.reqFs} (fallback si non supporte) ; `applySampleRate()` ferme+recree le contexte, re-acquiert le micro (si voulu) et reconfigure (A/C, correction, bande rebuildes au nouveau fs). Persiste (reqFs), verrouille en mesure ET en replay, toast de confirmation (+ avertissement si l'appareil n'applique pas le fs demande). Interet NVH basse frequence : fs bas -> Nyquist plus bas mais meilleure resolution en bas + moins de CPU. i18n 'Frequence d'echantillonnage'.

- **2.0.93-beta** : (1) l'onglet **Calibration** du menu **ouvre directement l'ecran de calibration** (setMenuCat branch 'calib' -> S.screen='cal' ; ligne mCalib + sa note retirees ; handler mCalib supprime). L'onglet de filtre courant (Signal/Affichage/Systeme) reste actif au retour. (2) **Traduction** des onglets (Signal/Affichage/Calibration/Systeme) et des notes du menu via table dediee UITR dans translatePass (isolee du moteur de tokens pour eviter les traductions parasites) ; EN/ES appliques au chargement (ligne setLang init).

- **2.0.92-beta** : REFONTE ERGONOMIE MENU EXPERT (etape 1). (1) **Onglets** en haut du menu (barre collante translucide) : **Signal / Affichage / Calibration / Systeme** ; chaque ligne porte data-cat, `setMenuCat()` filtre l'affichage (defaut Signal). Signal=FFT/fenetre/points/resolution/recouvrement/moyennage/lissage/tiers/niveaux IIR-FFT/passe-haut/passe-bas/interp pic/seuil emergence ; Affichage=vitesse spectro/lissage 3D/ecoute pente+gain/theme ; Calibration=ouvre l'ecran cal ; Systeme=reinit parametres+credit. (2) **Bloc 'Effacer' retire** (spectrogramme/evolution/tout = doublons de RESET + reset auto par mesure). (3) **Reset max/min/peak-hold + LCpeak par TAP sur l'afficheur** (cellules .resettable LAFmax/LAFmin/LCpeak, indice ↺, toast) ; clearPeak() etendu au lcPeak. (4) **Touche CALIB. allegee** : point colore (gris=desactive/bleu=defaut/vert=profil) + offset (ex. '● 130 dB') au lieu de 'Defaut+off · 130.0'. Metrologie inchangee (tests OK).

- **2.0.91-beta** : libelles d'appareil propres via helper `devLabel()` (iphone->iPhone, ipad->iPad, android->Android, desktop->PC). Applique au bouton Gain frequentiel ('Defaut PC'…), au titre de courbe ('Defaut PC (plate)' sur desktop, la generique desktop etant plate 0 dB), a la ligne 'Appareil detecte' et a l'export CSV (Correction; 'defaut PC' au lieu de 'auto desktop'). Sur PC sans profil : bouton = 'Desactive', cycle -> 'Defaut PC' (plate) -> 'Desactive' (Profil perso saute).

- **2.0.90-beta** : CLARIFICATION CALIBRATION (affichage + libelles, metrologie inchangee). (1) Bouton 'Correction amplitude' renomme **'Gain frequentiel'**, etats **Desactive / Defaut <appareil> (ex. iPhone) / Profil perso** (cpCorr). (2) Le graphe de la courbe (drawCurve) affiche desormais le **GAIN TOTAL rapporte au dBFS de BASE** (0 dB = base, iPhone 130) : gain(f) = (offset - 130) + forme. -> l'offset apparait comme un DECALAGE VERTICAL (offset 128 -> tout descend de 2) ; desactive -> ligne PLATE a (offset-base). Legende '0 = 130 dBFS (iPhone) · offset X → +/-Y dB'. (3) Offset etiquete 'selon modele (iPhone ~130), a caler au calibreur'. (4) Aide MAJ. La correction APPLIQUEE reste inchangee (forme 0 dB @ 1 kHz + offset scalaire) : bancs test_* toujours OK.

- **2.0.89-beta** : LIMITATION DE BANDE reglable. Passe-haut (Off/3/10/20 Hz, defaut 10) + passe-bas (Off/20/22 kHz, defaut 22k), biquads Butterworth 2e ordre (rbjHP/rbjLP, buildBandLimit). Applique au signal EN AMONT des ponderations dans lvlDet (det.bl) -> niveaux/Leq/LCpeak bornes (mode IIR, exact) ; masque spectral par bin S.blGain applique aussi aux boucles FFT (raies+bandes) et a la colonne spectrogramme -> spectre borne de facon coherente. Selecteurs menu Expert (mHP/mLP), persistes, verrouilles en mesure, reconstruits dans configureAnalyser (bl + blGain, sans reset integrateurs). Aide FR/EN/ES. Test dedie `tests/test_bandlimit.js` (10 controles : -3 dB aux coupures, pente 2e ordre, attenuation reelle LZeq, infrason rejete, neutralite Off). PROCEDURE GLOBALE mise a jour (make_global_report.py) : integre test_bandlimit + section/courbe bande, et genere desormais un **PDF format CETIM** (HTML stylé CETIM + wkhtmltopdf). deploy.py etendu pour pousser les binaires (blobs base64) -> PDF verse dans tests/.

- **2.0.88-beta** : (1) PERF spectrogramme 3D — le glissement live tournait a ~100 FPS avec un degrade recalcule par tranche a chaque image (forte charge GPU/GC -> echauffement -> lag progressif 'au bout d'un moment'). Cadence de glissement plafonnee a ~35 FPS (mouvement interpole par phase fractionnaire, donc toujours fluide) + degrade allege (stops tous les 4 points au lieu de 2). (2) PROCEDURE DE VALIDATION GLOBALE : `tests/make_global_report.py` orchestre en une commande les 3 bancs (test_weight/test_corr/test_realtime) + export donnees + courbes (plot_figs.py) + rapport GLOBAL .docx/.html (Partie I SONO-VAL-001 existante + Partie II SONO-VAL-002). Ref globale SONO-VAL-000. Scripts verses : export_data.js, plot_figs.py, make_global_report.py.

- **2.0.87-beta** : BANC DE VALIDATION TEMPS REEL `tests/test_realtime.js` — rejoue la boucle bloc par bloc avec les vraies fonctions (fft, aWeight/cWeight, Blackman, Parseval K cote FFT ; buildLvlDet/lvlDetRun cote IIR) et compare LAeq/LCeq/LZeq entre **IIR temporel**, **derive FFT** et **theorie analytique** (tons, multiton, bruit rose ; correction Off et profil MEMS). RESULTAT CLE : IIR vs theorie <=0,22 dB (Off ET correction active) -> le temps reel temporel est exact ; FFT vs theorie serre en Off mais s'ecarte sous correction (jusqu'a ~7 dB sur LZeq) par CANCELLATION DE PARSEVAL (K=Pt/pZ recale sur le RMS brut -> la contribution globale de la courbe s'annule) => confirme la necessite de l'Option 1 (Leq temporel quand correction active). 27 controles OK. Aide (selecteur) precisee : en mode Derive FFT sous correction, le niveau absolu perd la correction (Parseval).

- **2.0.86-beta** : correctif reel du defilement sur le graphe de correction. Une regle CSS GLOBALE `canvas{touch-action:none}` (ligne ~286) s'appliquait a tous les canvas ; retirer touch-action de #curveCv le faisait retomber sur cette regle -> toujours bloque. Fix : `#curveCv{touch-action:pan-y}` (surclasse la regle globale) -> le doigt fait defiler le menu au-dessus du graphe. (Piege a retenir : ne pas juste retirer touch-action d'un canvas, il faut l'ecraser explicitement.)

- **2.0.85-beta** : le graphe de correction (#curveCv, affichage seul) bloquait le defilement du menu Calibration quand le doigt passait dessus. Retire `touch-action:none` (CSS) et le listener `touchmove preventDefault` -> le doigt fait desormais defiler le menu normalement au-dessus du graphe.

- **2.0.84-beta** : graphe de la courbe de correction — retrait TOTAL de l'offset. Avant, drawCurve tracait courbe+offset avec une base usine `OFF0=120` -> a 130 dBFS il affichait une ligne 'offset +10.0 dB' (130-120) et decalait toute la courbe vers le haut. Desormais le graphe montre UNIQUEMENT la forme (activeCorrCurve, 0 dB a 1 kHz) : plus de ligne pointillee d'offset, plus de suffixe 'offset recale' dans le titre, bornes/points/trace sans +off. L'offset (scalaire, reference 0 dBFS) reste visible ailleurs (bouton CALIB, ecran offset) mais ne deforme plus la courbe.

- **2.0.83-beta** : la courbe de correction est desormais NORMALISEE a 0 dB en medium (1 kHz). Nouveau helper `activeCorrCurve()` = courbe active (generique OU profil) recalee g-=g(1kHz), renvoie null si Off. Branche sur compGain (spectre), buildCorrFilter (filtre temporel) ET le graphe -> les deux chaines voient la meme forme, 0 dB a 1 kHz. IMPORTANT : corrige aussi une incoherence : en mode 'generic', S.corrCurve etant null, buildCorrFilter renvoyait null -> les NIVEAUX n'etaient PAS corriges (seul le spectre l'etait) ; desormais le mode generique corrige les deux chaines (Option 1). Le niveau global reste porte par l'offset (130 dBFS par defaut), jamais par la courbe. Aide MAJ (forme normalisee 0 dB a 1 kHz).

- **2.0.82-beta** : DECOUPLAGE offset <-> profil de correction. Avant, applyProfile faisait `if(p.offset!=null)S.offset=p.offset` -> charger un profil (souvent cree quand le defaut etait 120) ramenait l'offset a l'ancienne valeur, ecart de ~10 dB alors que l'utilisateur etait sur 130. Desormais un profil = COURBE (forme frequentielle) uniquement ; l'offset (reference 0 dBFS, defaut iPhone 130) reste independant, modifie seulement par le calibreur ou Reinit. offset. La courbe generique iPhone (+12..+10 dB en grave, COMP_G_DEF) est une FORME, distincte du scalaire 130.

- **2.0.81-beta** : reference pleine echelle par defaut 0 dBFS = **130 dB SPL** (avant 120) — hypothese adaptee au micro iPhone. Modele : L = 10log10(ms) + S.offset (offset = SPL a 0 dBFS RMS). Change : defaut offset 120->130, bouton Reinit (130 dB), offView/cpOff, seuil badge offd (|offset-130|>=0.05), handler offReset. Rappel : offset persiste -> n'affecte que install neuve/reset ; un calibreur remplace l'hypothese par une valeur exacte. Aide Calibration MAJ (ref 130 par defaut + effet d'un ecart = decalage UNIFORME des niveaux absolus, sans toucher au relatif).

- **2.0.80-beta** : (1) SELECTEUR diagnostic 'Niveaux temps reel' (menu Expert, `mLvlSrc`, S.lvlSrc, persiste, verrouille pendant mesure) : **Temporel IIR** (defaut) vs **Derive FFT**. En mode FFT, S.pF/pS/pV lisses exponentiellement (F/S/V) depuis pA/pC/pZ (etat S._fft, reset begin/resetMeasure) et Leq via FFT (tdLeq&&lvlSrc!=fft) ; LCpeak reste du detecteur temporel. IIR inchange. (2) TEST GABARIT A/C : `tests/test_weight.js` compare designAWeight/designCWeight aux valeurs nominales IEC 61672-1 + tolerances classe 1/2. Resultat : **classe 1 de 16 Hz a 12,5 kHz** (DA/DC max 0,80 dB @44,1k, 0,65 dB @48k) ; 16 kHz = classe 1 @48k / classe 2 @44,1k ; 20 kHz hors (limite bilineaire pres de Nyquist, impact negligeable A/C). (3) Aide (FR/EN/ES) : rubrique Traitement du signal completee (selecteur + conformite classe 1).

- **2.0.79-beta** : (1) aide in-app — nouvelle rubrique **Traitement du signal (avance)** (FR/EN/ES, 17e section) : 2 chaines FFT/temporelle, Parseval+fenetre, raie tonale (-10log10 ENBW + interpolation curseur), tiers FFT vs IEC 61260, ponderations A/C IEC 61672, filtre de correction biquads, Leq corrige, validation. Rangee dans le groupe 'Analyse spectre' (HELP_GROUP_OF 17 entrees ; Precision->groupe 0). (2) bouton '?' d'aide qui chevauchait le mot SONOMETRE : interlettrage .16->.11em + margin-right 8px sur .wordmark + margin-left 2px sur .helpbtn. (3) doc GitHub (MEMOIRE 7ter) completee : amplitude FFT/fenetre/Parseval + raie tonale.

- **2.0.78-beta** : routine de test du filtre de correction (`tests/test_corr.js`, versionnee dans le depot V2) + AFFINAGE du fit. designCorrFilter : maillage revu (low-shelf 22 Hz + peaks 31,5 Hz->15 kHz ~1/2 oct avec HF densifie 10k/12,5k/15k + high-shelf 15 kHz), **Q=1,4** (recouvrement large -> fit lisse, faible ondulation), ridge 0,06, 8 iterations, 22 biquads. Fidelite (validee Node, fs 44,1/48k) : MEMS **0,18 dB**, boost 0,08 dB, courbe extreme 0,41 dB sur 50 Hz-12,5 kHz (avant ~1 dB). test_corr.js : 49 controles OK (fidelite 4 courbes, neutralite Off, gain reel sur tons purs, coherence energetique Z, coherence chaine corr->A, stabilite bruit blanc, reference A). A rejouer apres toute evolution du filtre.

- **2.0.77-beta** : CORRECTION MICRO EN TEMPS REEL (Option 1). Nouvelle chaine : filtre de correction TEMPOREL (cascade de biquads RBJ : low-shelf + peaks ~1/2 oct + high-shelf, gains par moindres carres linearises, 5 iterations sur residu ; designCorrFilter/buildCorrFilter). Insere dans lvlDet AVANT A/C -> LAF/LAS/LV, LAFmax, LCpeak ET Leq refletent la correction, sans latence. Leq desormais derive du SIGNAL TEMPOREL CORRIGE quand corr!=Off (det.eqA/eqC/eqZ/eqN -> S.eq ; bascule tdLeq=(corr!=off)&&!REPL.on) ; corr=Off inchange (FFT/Parseval). Filtre reconstruit en place dans configureAnalyser (sans reset integrateurs). Fidelite courbe MEMS : maxErr bande utile 1,03 dB @31 Hz, <0,5 dB de 50 Hz a 12,5 kHz (valide Node fs 44,1/48k). Repli FIR non implemente (hors domaine des courbes MEMS). Aide (Calibration) + MEMOIRE (sections 7bis validation + 7ter chaine de traitement) mises a jour. Spectres inchanges (compGain par raie).

- **2.0.76-beta** : pendant l'INIT micro (S.running && warmT>0 && !REPL.on), AUCUNE courbe affichee. drawSpectrum : grille vide + axes puis return. sgTick : return avant d'ajouter des colonnes (spectrogramme vide aussi). La courbe apparait quand la mesure demarre reellement.

- **2.0.75-beta** : etiquettes curseur bleu et emergence raccourcies et sur DEUX lignes. labelBox accepte un tableau de lignes + retourne sa hauteur (empilement labY dynamique). Curseur : L1 'freq Hz · niveau dBx ⟂', L2 'emg +x.x dB' (si emergence). Emergence : L1 'emg +x.x dB', L2 'freq Hz' ('emergence'->'emg').

- **2.0.74-beta** : halo du lisere LED (rouge rotation/pause, vert fin) accentue : 4 drop-shadow (jusqu'a 48-50px de rayon) au lieu de 3, lueurs plus intenses.

- **2.0.73-beta** : EMG actif = ambre PLEIN comme MOY (vert plein). Retrait de l'override #emgBtn.on translucide (.24) -> EMG utilise .sg3dbtn.on (ambre plein). MOY reste vert plein (#avgBtn.on).

- **2.0.72-beta** : logique boutons overlay refaite PAR BOUTON (independants). Base .sg3dbtn opacity .4 (toujours semi-transparent), classe .flash -> opacity 1 pendant 1,5 s. flashBtn/unflashBtn. Clic : activation -> flash 1,5 s puis re-transparent ; desactivation -> unflash immediat. Applique a avgBtn/emgBtn/sg3dBtn/lsnBtn. Retire l'ancien systeme global (body.sgbtn-dim, fadeWake, tap-anywhere). BUG CORRIGE : #avgBtn.on etait redefini en BLEU (ecrasait le vert) -> supprime, MOY actif enfin VERT. #sg3dBtn/#lsnBtn opacity .75->.4 (uniforme).

- **2.0.71-beta** : logique fade boutons MOY/EMG clarifiee. De base estompes (.4) ; un clic sur l'un toggle son etat + fadeWake (les deux pleinement visibles, aura de couleur sur l'actif) ; ~2,2 s apres, les DEUX re-estompent ensemble (plus d'exception .on qui gardait l'actif a .9 -> l'actif s'estompe aussi mais garde sa teinte). fadeWake ajoute dans avgBtn/emgBtn onclick pour reveil fiable au clic. Delai 3500->2200 ms.

- **2.0.70-beta** : MOY/EMG toujours OFF + estompes au lancement. (1) defaut emgOn:true -> false. (2) 'emgOn' et 'specAvg' retires de PREF_FIELDS -> etat on/off non memorise, toujours off au demarrage (emgThr reste persiste). (3) DOMContentLoaded ajoute body.sgbtn-dim d'emblee (avant fadeWake au 1er lancement laissait les boutons visibles 3,5 s) -> boutons estompes des l'init, reveil a la 1re interaction.

- **2.0.69-beta** : lisere LED (rouge rotation/pause, vert fin) = halo lumineux doux, triple drop-shadow (rayonnement leger) au lieu d'une seule ombre.

- **2.0.68-beta** : fix estompage boutons overlay. Avant : classe 'fade' posee PAR bouton -> desync (EMG affiche seulement en bande fine reapparaissait sans fade, MOY pouvait se re-afficher seul). Maintenant : classe GLOBALE body.sgbtn-dim (fadeWake la bascule) + CSS body.sgbtn-dim .sg3dbtn{opacity .42}/.on{.9}. Tous les boutons s'estompent/reveillent ENSEMBLE, quel que soit le moment ou ils apparaissent ; plus de reactivation intempestive de MOY.

- **2.0.67-beta** : .kf 7->6.5px. Boutons overlay .sg3dbtn plus petits : 46x26 -> 40x22, font 11->10px ; emgBtn repositionne right 60->54px (sg3dBtn/lsnBtn restent a gauche via CSS). MOY actif vert, EMG ambre (inchange).

- **2.0.66-beta** : .kf (sous-texte touches) 7.5->7px. Etat actif differencie par bouton : MOY (#avgBtn.on) = VERT vif #2FD98A ; EMG et autres restent ambre (.sg3dbtn.on).

- **2.0.65-beta** : boutons overlay (MOY/EMG) plus reperables en mode estompe : .sg3dbtn.fade opacite .18 -> .42 ; bouton ACTIF reste bien visible meme estompe (.sg3dbtn.on.fade opacite .9).

- **2.0.64-beta** : bouton overlay actif (MOY/EMG) passe en AMBRE vif #FFA92E (vert pas assez visible) : texte fonce + halo ambre, tres contraste sur l'ecran.

- **2.0.63-beta** : fin de mesure = 2 clignotements LEGERS verts (peaks .75) puis liseré VERT STABLE (opacite .5) MAINTENU jusqu'a la prochaine mesure. ledDone remanie (finit a .5, animation forwards one-shot -> pas de CPU continu). Retrait du setTimeout : rec-done reste jusqu'a kStart/resetMeasure/replayStart.

- **2.0.62-beta** : aide regroupee en 4 GRANDS THEMES cliquables (onglets) au lieu de 15 puces. HELP_GROUP_OF mappe chaque section (par ordre du document, identique 3 langues) vers un groupe : 0 Prise en main (Demarrer/Ecrans/Reglages rapides/Gestes), 1 Mesure & indices (Indices/Declenchement/Calibration/Emergence), 2 Analyse spectre (Tiers d'octave/Type de spectre/Vue 3D), 3 Reglages & export (Export/Themes/Preferences/Hors ligne). showHelpGroup n'affiche que les sections du groupe (dataset.grp sur h3+contenu jusqu'au h3 suivant). Intitules traduits (HELP_GROUPS fr/en/es). Onglet actif surbrillance verte. Ouvre sur groupe 0.

- **2.0.61-beta** : (1) bouton overlay actif (MOY/EMG...) desormais VERT bien visible (avant bleu translucide peu lisible) : .sg3dbtn.on fond vert #2FBF83, texte fonce, halo vert. (2) fin de mesure : l'anneau vert CLIGNOTE 2 fois (keyframes ledDone remaniees, duree 1.5s, retrait a 1600 ms). (3) textes des touches reduits pour gagner en hauteur graphe : .kf 8->7.5px, .kv 14->13px, .key min-height 41->38px, gap interne 2->1px.

- **2.0.60-beta** : aide appli — barre de SOMMAIRE cliquable en haut (#helpNav) : une puce par section (h3), construite dynamiquement -> defile la zone d'aide vers la section (buildHelpNav appele a l'ouverture + apres changement de langue, marche FR/EN/ES). helpbody passe en flex:1/min-height:0. MAJ contenu aide FR : Demarrer (init micro seulement 1re mesure + chrono a 00:00 pendant init + anneau vert de fin), Gestes (spectrogramme double-tap/M/2 doigts = suivi du max, suit la lecture en replay), Themes (ajout Neon). NB pour Thibaut : MEMOIRE.md tenue a jour a chaque deploiement ; l'aide in-app etait mise a jour partiellement -> passe de rattrapage FR faite ; EN/ES non resync (le sommaire fonctionne quand meme).

- **2.0.59-beta** : (1) init micro (warm) quasi-instantane pour les mesures SUIVANTES : warmMax coldStart?2.5:0.05 (avant 0.6). 1re mesure = armement long (micro froid, AGC iOS) ; suivantes (micro deja actif, non libere par stopMeasure) ~50 ms. (2) chrono ne demarre plus pendant l'init : elapsed n'avance que si !warm (avant il comptait puis beginRun le remettait a 0 -> barre/chrono trompeurs pendant INIT). (3) FIN de mesure (duree atteinte, stopMeasure(auto)) : anneau VERT (body.rec-done, keyframes ledDone 2.4s) entoure l'ecran puis s'estompe ; retire au demarrage/reset. NB : apres un REPLAY le micro est libere -> prochaine mesure = froide (normal).

- **2.0.58-beta** : (1) graphes encore agrandis : clavier plus compact (.key 45->41px, keypad gap 6->5/padding 6-7/margin 6, .krow gap 5) + .lcd padding 10-12-8 -> 8-11-6 -> graphzone (flex:1) gagne en hauteur. (2) Game Boy : la grille de haut-parleur du haut n'est plus affichee mais l'ESPACE est conserve (.miccap i visibility:hidden au lieu de .miccap display:none) -> ecran aligne avec les autres themes. (3) LISIBILITE des popovers en themes clairs (Game Boy/Clair) : .cp-head/.cp-lbl/.langbtn/.micsel/hpVer utilisaient des variables de theme (vert fonce en Game Boy) sur fond marine fixe -> illisibles. Passes en couleurs CLAIRES fixes (#CFE4FF/#8FB2E8), valable tous themes puisque le fond des popovers est toujours sombre. Options du select restent sombre-sur-blanc (liste native).

- **2.0.57-beta** : suivi du Max (curseur auto) du spectrogramme retabli EN REPLAY. (1) replBuildSgram recalcule desormais le curseur auto sur la colonne AU CURSEUR de lecture (derniere colonne) -> suit la lecture (avant : maj seulement dans le chemin direct sgColumn). (2) handlers sgramCv (onTap/onTap2/onChange) redessinent via renderReplayFrame(REPL.i) quand REPL.on (sinon drawSgram). (3) DOUBLE-TAP (tactile) et DOUBLE-CLIC (PC) sur le spectrogramme activent/coupent le suivi du max (option dblTapMax) au lieu de reset vue ; deux doigts / Maj+clic / touche M marchent toujours. NB : reset vue du spectrogramme = pincer (double-tap reaffecte).

- **2.0.56-beta** : resolution frequentielle du spectre fin en RELECTURE rendue adaptative a la taille du fichier. Avant : grille fine reduite plafonnee a RES=320 lignes (budget 20 Mo, 40e6/(16*count)) quelle que soit la duree -> trop grossier. Maintenant RESmax=max(64,min(n,floor(48e6/(8*count)))) : budget ~48 Mo pour les 2 pistes fines (rawFine+cumFine, 8 o/element) divise par le nb de trames. Petits fichiers -> jusqu'a la PLEINE resolution FFT (RES=n) ; gros fichiers -> reduit auto mais bien > 320 (ex. 30s=plein, 1min~5000, 5min~1000, 20min~250 vs ~100 avant). finMap Int16 OK (RES<=n<=16384). Aucun surcout FFT (RES n'affecte que le stockage/bucketing O(n)).

- **2.0.55-beta** : (1) legende spectre qui chevauchait l'echelle : titre raccourci (SPECTRE · BANDE FINE · Z, 1/3 OCT ; 'POND.' retire) + .gtitle/.gscale bornes (max-width 56%/43%, nowrap, ellipsis). (2) boutons MOY/EMG actifs : surbrillance nette (.sg3dbtn.on bg .5 + bordure #7FD8FF + halo). (3) nouveau theme 'Neon' : touches a lisere lumineux (bordure+halo bleu), couleur qui vire au rose #FF3D6E a l'appui (transition), keypad avec halo ; key-start halo rouge. Ajoute a THEMES + retrait classe th-neon.

- **2.0.54-beta** : fix courbe bande fine qui debordait sur la zone des ordonnees. Cause : i0=floor(fmin/binHz) -> 1er point a une frequence < fmin -> x2 < padL (a gauche de l'axe). Correctif : clip du trace a la zone du graphe rect(padL,0,w-padL,h) autour du stroke. (Rappel : y2 clampe deja en vertical.)

- **2.0.53-beta** : ajustements mise en page. Grand chiffre principal .numwrap clamp(44,13vw,64)->clamp(38,11vw,54) (plus petit). Clavier plus compact : .key min-height 52->45px, .keypad gap 7->6/padding 9-10->7-8/margin-top 10->8, .krow gap 7->6 -> le bezel (flex:1) gagne en hauteur, ecran graphique plus haut.

- **2.0.52-beta** : preset Transitoire ramene a 2 moyennes (au lieu de 8) — peu de moyennage = reactif, coherent pour evenements brefs. Picker + aide MAJ (moyennage 2x). Tonal=2, Equilibre=4, Transitoire=2.

- **2.0.51-beta** : les presets pilotent aussi le moyennage/lissage du spectre. SG_PRESETS : tonal specFrames=2, transient specFrames=8, balanced specFrames=4 ; specSmooth=0 (lissage frequentiel OFF) pour tous. applyPreset applique specFrames/specSmooth + reset Welch. Defauts de base : specFrames=4, specSmooth=0. Menu Expert : 'Optimiser le spectrogramme' renomme 'Type de spectre' (titre popover 'TYPE DE SPECTRE', traduction MAJ, aide MAJ). Fix mise en page : #mPresetV en white-space:nowrap (tick + nom sur une meme ligne). Descriptions picker completees (moyennage 2x/8x/4x).

- **2.0.50-beta** : moyennage temporel du spectre passe en VRAI WELCH (avant : moyenne exponentielle par frame ecran ~16 ms -> recouvrement implicite ~95%, 'N trames' non representatif, incoherent direct/replay). Desormais : la trame FFT courante est capturee chaque frame dans curFin/curBnd, mais AGREGEE (moyenne des N derniers periodogrammes) seulement a PAS FIXE = fftN*(1-overlap)/fs via welchUpdate(ts). Somme glissante O(n) (ring de N + sommes sumFin/sumBnd). N=1 -> live pur (chaque frame). Affichage rafraichi chaque frame, spectre stable entre deux pas d'agregation. 'N moyennes' devient une vraie grandeur avec recouvrement defini (reglage overlap du menu Expert), coherente direct<->replay (replay = moyenne des N dernieres trames stockees au meme pas). S.welch reconstruit si N/n/nb changent ou reset. Lissage frequentiel (1/n oct) inchange (frequence pure, pas d'overlap).

- **2.0.49-beta** : debruitage spectre fin = 2 leviers pro reglables (menu Expert), independants de TEMPS. (1) MOYENNAGE TEMPOREL exprime en NOMBRE DE TRAMES S.specFrames (1=live,2,4,8) : moyenne exponentielle a=(N-1)/N sur binPowD/bandPowD (coherent quel que soit Δf, contrairement a une constante en s). (2) LISSAGE FREQUENTIEL S.specSmooth (0/6/12/24 = Off / 1/6 / 1/12 / 1/24 oct) : moyenne glissante 1/n octave sur la trace fine via sommes cumulees (O(n), AUCUNE latence), applique a l'AFFICHAGE seulement (curseur/emergence/maxCur restent sur binPowEff brut). Defaut specFrames=4, specSmooth=12. Replay coherent : binPowD/bandPowD = moyenne des N dernieres trames stockees (rawFine/F.band) ; lissage frequentiel applique au dessin comme en direct. Rappel theorie : 1 trame FFT = ~2 DOF/raie (~5.6 dB d'ecart-type sur bruit) -> il FAUT moyenner. PREF : specFrames/specSmooth.

- **2.0.48-beta** : SIMPLIFICATION spectre = LIVE + MOY, point. Retrait complet du maintien de crete MAX (bouton maxBtn, S.specMax, S.binPeak, dessins barres+fin) et du reglage de lissage d'affichage (S.specTau, ligne menu 'Lissage spectre', handler). Le spectre (bande fine + tiers/octave) affiche chaque trame telle quelle (S.binPowD/bandPowD = instantane, plus de lissage) ; MOY (Leq via binEnergy/bandEnergy) inchangee. TEMPS n'agit toujours que sur les niveaux globaux. REPLAY refactor coherent : on ne stocke plus les pistes fines/bandes lissees F/S/V (tFfine/tSfine/tVfine, tFband/tSband/tVband) — remplacees par F.rawFine (trame instantanee) ; les bandes viennent de F.band (brut). renderReplayFrame : binPowD=rawFine, bandPowD=F.band, MOY=cumFine. replBuildSgram : spectrogramme replay = rawFine (instantane, comme le direct via fftDb). Les pistes LARGE BANDE F/S/V (tFA..tVZ) restent (niveaux globaux/TEMPS). Memoire replay reduite. PREF : specTau/specMax retires.

- **2.0.47-beta** : le maintien de crête du spectre devient un OVERLAY OPTIONNEL 'MAX' (avant : traits de crête TOUJOURS affiches en tiers/octave, redescente 3 dB/s -> pic qui 'reste' apres arret du son = comportement signale). Nouveau bouton MAX sur le pane spectre (a cote de MOY/EMG), etat S.specMax (defaut OFF, persiste). Barres (tiers/octave) : traits de crete gates derriere specMax (+ exclus de l'autoRange si off). Bande fine : nouvelle courbe de maintien de crete (S.binPeak, maj lineaire pkDecay=10^(-0.3 dt)/frame ~3 dB/s) tracee seulement si MAX. Par defaut le spectre n'affiche donc plus que la trace live (lissage specTau) -> un sifflement qui s'arrete retombe proprement. MOY (Leq) inchange.

- **2.0.46-beta** : spectre DECOUPLE du bouton TEMPS. F/S/V ne pilotent plus que les niveaux globaux (correct IEC). Le spectre (bande fine + tiers/octave) a desormais son propre lissage d'AFFICHAGE, constante dediee S.specTau (defaut 0,5 s), reglable menu Expert 'Lissage spectre' : Live(0)/0,25/0,5/1 s. Nouveaux tableaux S.binPowD/bandPowD (une seule constante aD=exp(-dt/specTau) ; aD=0 si Live). MOY (Leq via binEnergy/bandEnergy) et Max-hold (bandPeak, base sur l'instantane) inchanges. Consommateurs repointes : drawSpectrum (fin+octave), emergence, specCurveDb, calibration (bandLevelNow/calDo1k). Replay : binPowD/bandPowD remplis depuis la piste Fast (proxy 'au curseur'). Spectrogramme deja instantane (S.fftDb) : inchange. specTau persiste (PREF_FIELDS). Rappel : les niveaux globaux (grand nombre, LAFmax, LCpeak, L10/50/90) restent en ponderation temporelle F/S/V (Option 2, 2.0.45).

- **2.0.45-beta** : OPTION 2 — niveau large bande a ponderation TEMPORELLE (decouple de la FFT). Les niveaux instantanes A/C/Z (grand nombre LAF/LAS/LV), LAFmax, LCpeak et percentiles L10/L50/L90 sont desormais calcules dans le domaine temporel sur les echantillons neufs (chemin continu deja utilise pour LCpeak, PAS d'AudioWorklet -> zero risque session micro iOS). Chaine : filtre A ou C (IIR biquad IEC 61672, nouveau designAWeight ; C = designCWeight existant) -> carre -> integrateur exponentiel PAR ECHANTILLON aux constantes Fast/Slow/V-Slow. Reponse conforme a tau sans latence de fenetre FFT. Leq, spectre, spectrogramme, tiers d'octave restent sur la FFT (inchanges). Detecteur = S.lvlDet (buildLvlDet/lvlDetRun/runBq), re-cale a chaque mesure via resetMeasure (S.lvlDet=null). Validation node (det_test.js + design_biquad.js) : filtre A/C ecart <=±0.65 dB de 16 Hz a 12.5 kHz vs IEC (deviation bilineaire 16-20 kHz documentee, sans impact BF) ; 1 kHz -20 dBFS -> 74.00 dB exact ; A(100 Hz)=-19.15 (IEC -19.1) ; chute Fast/Slow = 4.3 dB apres 1 tau (=10log10 e, exact). CAVEAT : la correction micro frequentielle (S.compGain, corr!='off') ne s'applique PLUS au niveau instantane temporel (elle reste sur spectre/bandes/Leq via FFT) ; sans effet quand corr='off' (cas courant). A confronter au banc DSP (chantier 5) avant fusion main.

- **2.0.44-beta** : ecran Valeurs, chiffres rognes corriges. Cellules passees en flex centre (align/justify center) + tailles RESPONSIVES clamp() au lieu de px fixes : hero .v clamp(26px,8vw,40px) ; .v clamp(15px,4.8vw,20px) ; .v.small clamp(11px,3.4vw,13px). line-height ajuste (1.08/1.1/1.25). Plus de rognage haut/bas quel que soit l'ecran.

- **2.0.43-beta** : en PAUSE d'enregistrement, le lisere LED cesse de tourner et clignote legerement. Nouvel etat `body.rec-paused` (toggle dans ui() : S.running && S.paused && !REPL.on ; retire dans stopMeasure). CSS : anneau rouge uniforme (conic-gradient statique) + keyframes ledBlink (opacite .22->.7, 1.5s). rec-on et rec-paused mutuellement exclusifs. Reduced-motion : anneau statique en pause.

- **2.0.42-beta** : differenciation du bouton PAUSE selon le contexte (setTransport). ENREGISTREMENT en pause : garde l'icone pause (plus de triangle vert), effet halo pulse `.pausedrec` (pauseGlow + bars clignotantes). LECTURE (replay/fichier charge) : bouton VERT a triangle BLANC `.playgreen`. L'ancien style `.paused` (ambre + triangle vert) n'est plus applique. Cle de cache _transport enrichie (gp/gl/rp/rr).

- **2.0.41-beta** : lisere LED rotatif autour du bezel comme indicateur d'enregistrement (leger). Comete rouge parcourant le contour via `.bezel::before` (anneau conic-gradient masque padding + mask-composite exclude, angle anime par @property --ledA, keyframes ledSpin 2.6s). Active par `body.rec-on` (toggle dans ui() : S.running && !paused && !REPL.on && !warm ; retire dans stopMeasure). Repli prefers-reduced-motion : pulsation douce. Pas en INIT/pause/replay.

- **2.0.40-beta** : lisibilite PC des listes deroulantes micro/sortie/gain (.micsel). La liste native ouverte s'affichait sur fond blanc avec texte clair herite (var(--lcd-ink)) = illisible. Ajout `.micsel option{color:#0A1F42;background:#fff}` + `.micsel{color-scheme:light}`. Correctif CSS seul.

- **2.0.39-beta** : (1) MESURE Réinit efface desormais AUSSI le curseur du spectre (S.cursor) et le suivi de max (S.maxCur) — ajoutes dans resetMeasure (les curseurs spectro/evolution etaient deja effaces). (2) Appui LONG sur DUREE ouvre un sous-menu (popover durPop) : presets ∞/10s/30s/1min/5min/15min + champ duree PERSONNALISEE en secondes (borne 24 h ; 0/vide = ∞). Appui COURT sur DUREE = cycle rapide inchange. Nouveau formateur durLabel() gere l'affichage des durees perso hors liste (kDurV + tLine). Popover verrouille si mesure/replay en cours.

- **2.0.38-beta** : completion gestes PC. Ajout des equivalents souris/clavier manquants : tableau Valeurs (molette, fleches gauche/droite, clic sur les points = changer de page), curseur max auto via Maj+clic (equivalent deux doigts / onTap2), raccourcis clavier Espace=demarrer/arreter, R=reset vue, M=curseur max. Doc (aide Gestes) completee dynamiquement pour PC via applyInputHints. Raccourcis ignores si un champ de saisie a le focus.

- **2.0.37-beta** : consignes adaptees PC vs tactile. Detection IS_PC (matchMedia hover:hover + pointer:fine). Si PC, applyInputHints() reecrit l'aide Gestes (molette=Y, Ctrl+molette=X, clic-glisser=deplacer, clic=curseur, double-clic=reset), la note du menu dynamique, et le texte incruste du spectrogramme. Ajout double-clic PC = reset vue / retrait curseur dans attachZoomPan (equivalent du double-tap tactile). Tactile : libelles inchanges.

- **2.0.36-beta** : **support souris PC** sur les 3 graphes. Convention orientation-écran : **molette = axe Y (vertical)**, **Ctrl(ou ⌘)+molette = axe X (horizontal)**, **clic-glisser = comme un doigt** (pan/curseur). Mapping : spectre → Y=dB (zoom, amorcé depuis l'auto), X=fréquence ; spectrogramme → Y=fréquence, X=temps ; évolution → Y=dB, X=temps, glissé=déplace le curseur. Souris synthétique post-tactile (iOS) ignorée (fenêtre 700 ms) ; clic supprimé après un glissé. Ancienne molette (Shift=temps) remplacée.

- **2.0.35-beta** : **CORRECTIF débordement LAeq (écran Valeurs)**. Sur iPhone le grand nombre LAeq débordait de sa case et chevauchait LCpeak/LAFmax. Cause : auto-agrandissement de police iOS (`text-size-adjust` non défini) qui gonflait le nombre héro. Correctif : `-webkit-text-size-adjust:100%` sur html/body + `line-height:1` sur la valeur héro, `line-height:1.05` sur les valeurs, `overflow:hidden` sur les cellules (anti-débordement). **Porté aussi en V1 (1.35.48)** — même bug en production.

- **2.0.34-beta** : **CORRECTIF filtre d'écoute ♪ en replay**. `lsnTick` ne rafraîchissait le filtre que si `!S.running` ; or en replay `S.running` est vrai et `loop()` (qui met à jour le filtre en live) NE tourne pas → le filtre ne suivait plus le zoom fréquentiel du spectrogramme. Corrigé : `lsnTick` appelle `lsnUpdate` aussi quand `REPL.on`. La bande passante de l'écoute suit de nouveau la fenêtre zoomée en réécoute.

- **2.0.33-beta** : section SORTIE affiche le **nom de sortie détecté** s'il est exposé (lecture seule sur iOS), sinon message clair "iOS n'expose pas le périphérique de sortie". enumerateDevices ne renvoie en général que les entrées sur iOS → le nom de sortie est le plus souvent indisponible ; l'app le dit honnêtement plutôt que d'afficher un faux "Automatique".

- **2.0.32-beta** : **anti-larsen réécoute ♪ en mode micro + routage replay**. (1) En mode MICRO, activer ♪ demande une **confirmation explicite** (annulable) rappelant d'écouter au CASQUE — iOS/Safari ne permet ni de forcer la sortie casque ni de détecter les écouteurs (setSinkId non supporté), donc la confirmation est le seul garde-fou fiable côté web. En RELECTURE, aucune confirmation (pas de micro → pas de larsen, haut-parleur OK). (2) Au démarrage d'un replay, le **micro est libéré** (tracks stoppés) pour qu'iOS quitte la session enregistrement+lecture et route correctement la réécoute (souvent forcée sur HP quand le micro reste actif) ; réacquis au prochain REC.

- **2.0.31-beta** : **sélecteur de SORTIE (réécoute)** ajouté sous le sélecteur micro dans le menu CALIBRATION. Peuplé via enumerateDevices (audiooutput) ; appliqué au contexte via `AudioContext.setSinkId` si supporté (Chrome/Edge desktop) → `applyOutChoice`, persistant (`S.outId`), réappliqué à la création du contexte. **iOS/Safari ne supporte pas setSinkId** → sélecteur désactivé + note "la sortie est gérée par le système (écouteurs si branchés, sinon haut-parleur)". Honnête sur la limite iPhone.

- **2.0.30-beta** : **CORRECTIF raies fantômes (grille fine adaptative)**. La grille réduite log fixe avait, en bas du spectre, plus de lignes que la FFT n'a de points → lignes vides = raies fantômes (visibles surtout en log). Nouvelle grille ADAPTATIVE : 1 point FFT par ligne tant que le pas log est plus fin que la FFT (→ pleine résolution ~5,9 Hz/ligne jusqu'à ~200 Hz, essentiel pour l'analyse basse fréquence), puis regroupement progressif en haut. Zéro ligne vide → zéro fantôme, en lin comme en log. Spectrogramme mappé sur la MÊME grille (via finMap du bin central) pour cohérence. Test : RES=201 lignes, 0 vide, 1 bin/ligne jusqu'à 200 Hz.

- **2.0.29-beta** : **spectrogramme suit le curseur (rembobinage) + fin de l'accumulation**. Avant : le spectrogramme n'était qu'alimenté en lecture (via sgTick/S.fftDb) → il s'empilait à chaque relecture et ne rembobinait pas. Maintenant `replBuildSgram(i)` le **reconstruit** à chaque rendu depuis les spectres fins réduits stockés (piste selon TEMPS) : fenêtre de trames finissant à l'index → suit/rembobine avec le slider, et jamais d'accumulation (reconstruit, pas empilé). Tables row→ligne réduite + poids de pondération par ligne mises en cache (clé Hb/géométrie/pondération/RES).

- **2.0.28-beta** : **spectre bande fine suit le curseur + MOY bande fine** (stockage par trame, résolution réduite). Grille log `RES` lignes (≤256, bornée par budget ~40 Mo → RES plus faible sur enreg. longs). Dans la passe de calcul, chaque trame réduit le spectre FFT en `RES` lignes (moyenne de puissance recalée Parseval) et alimente 4 pistes flat : **cumFine** (MOY cumulative début→trame) + **tFfine/tSfine/tVfine** (lissées Fast/Slow/V-Slow). `renderReplayFrame` étend la piste choisie vers `S.binPowF/S/V` (via `finMap`) et remplit `S.binEnergy` depuis cumFine → le spectre fin **suit le curseur** (instantané lissé selon TEMPS) et la **MOY fine** redevient exacte (début→curseur), en réutilisant le tracé existant. `fftDb` gardé net (FFT à la volée) pour émergence/pics. Neutralisation `_binEn` supprimée. Limite restante : spectrogramme figé pendant le scrub (rembobinage = étape suivante si besoin).

- **2.0.27-beta** : **Fast/Slow/V-Slow actifs en replay** (pré-calcul temporel). 2e passe dans `replBuildFrames` : filtre exponentiel (α=exp(−50ms/τ), τ = 0,125/1/10 s) déroulé sur les trames → 3 pistes lissées A/C/Z (broadband + tiers d'octave) stockées (~2 Mo pour 5 min). `renderReplayFrame` lit la piste selon TEMPS (au lieu de l'instantané figé) : la valeur principale, les tiers d'octave et la courbe Évolution suivent la constante choisie. **TEMPS/Pondération/Correction DÉVERROUILLÉS en replay** (changement instantané, sans ré-analyse pour W/T ; Correction relance l'analyse) ; Durée verrouillée. NB : l'app n'a pas de mode Impulse (le 3e mode est V-Slow τ=10 s) — un vrai Impulse serait une fonctionnalité à ajouter côté direct+replay. Mémoire/spectrogramme-rembobinage + MOY bande fine = brique suivante.

- **2.0.26-beta** : **CORRECTIF légende + curseur Évolution** — cause racine trouvée. La ligne de légende de `drawHistory` lisait `useAvg`, variable qui n'existe QUE dans `drawSpectrum` → **ReferenceError** : les courbes (tracées avant) s'affichaient, mais légende ET curseur (après) ne s'exécutaient jamais. Légende passée en `L{pondération}{temps}` (le graphe trace le L instantané). **Bug présent aussi en V1 → porté sur main (1.35.47)** : même correctif + curseur Évolution robuste (ligne toujours visible au point touché, comportement identique au curseur Spectre : tap pose, glisse déplace, double-tap efface, valeurs L/Leq).

- **2.0.25-beta** : **curseur Évolution robuste** (bug PRÉEXISTANT, présent aussi en V1). La ligne du curseur & tout son bloc étaient dans `if(best)` → si aucun point de l'historique ne tombait dans la fenêtre visible au point touché, RIEN ne s'affichait (pas même la ligne). Désormais la ligne verticale se dessine toujours au point touché ; dots + label ajoutés si un point existe. NB : la panne "micro/courbes" signalée était un service worker/cache résiduel (cf. 2.0.24, confirmé : OK en navigation privée), pas le code.

- **2.0.24-beta** : **beta auto-nettoyant (correctif "micro bloque/pas de courbes")**. Diagnostic : le code 2.0.23 est sain (chargement + REC OK en simulation jsdom) ; le symptome venait d'un **service worker residuel** de l'app principale (anterieur a l'exclusion de /beta/) qui reservait une VIEILLE copie du beta en ignorant le ?v=. Desormais la page beta, au load, **desenregistre tout SW + vide les caches** puis se recharge une fois (garde sessionStorage betaPurged). Le SW de prod se reinstalle a la prochaine visite de l'app principale.

- **2.0.23-beta** : **CORRECTIF 1er stop** — le bouton repassait en "record" et perdait le ▶ vert. Cause : `replayTeardown()` finit par `ui()` alors que `S.running` était encore vrai & `REPL.on` déjà faux → la ligne de nettoyage "mesure micro" libérait buf/frames, donc `replFileLoaded()` devenait faux. Correctif : `replExit` met `S.running=false` AVANT le teardown. Résultat 1er stop : slider à 0, **bouton principal reste STOP**, **touche lecture ▶ verte prête**, Pondération/Temps cliquables, fichier gardé.

- **2.0.22-beta** : **stop en deux temps repensé**. **1er stop** : quitte le mode replay → retour à 0, **Pondération/Temps réactivés immédiatement**, fichier CONSERVÉ (barre visible, figure gelée au début, ▶ relance). **2e stop** : **décharge le fichier** (buf/frames/meta libérés), masque la barre, **réinitialise toutes les figures** (resetMeasure). Transport découplé de S.running : `replFileLoaded()` garde le bouton stop + la touche lecture actifs tant qu'un fichier est chargé ; `setLocks` ne verrouille que pendant mesure micro ou replay ACTIF. Démarrer une mesure micro décharge d'abord le fichier. `replExit`/`replUnload` ajoutés.

- **2.0.21-beta** : **retrait du bouton ⟲** (rembobinage devenu inutile : stop rembobine déjà à 0 avec ▶ prêt) — HTML/CSS/handler supprimés. **Sortie du replay (2e stop) masque la barre** et déverrouille Pondération/Temps (via setLocks/S.running=false). La barre n'apparaît donc que pendant un replay actif.

- **2.0.20-beta** : **stop en replay affiné**. 1er stop (en lecture ou curseur >0) → rembobine à 0:00, met en pause, **bouton lecture ▶ prêt** ; 2e stop (déjà à 0) → quitte le replay (retour micro/repos). Pondération et Temps restent **verrouillés** pendant tout le replay, lecture comme pause (déjà via setLocks/S.running). Fin naturelle → ▶ prêt (relance depuis le début).

- **2.0.19-beta** : **ré-analyse auto au changement de réglage d'analyse**. Pendant un replay, changer **fenêtre / taille FFT / préréglage / correction micro** (tout ce qui passe par `configureAnalyser`) déclenche un toast « ré-analyse… » et **relance la passe à froid depuis le début** (`replReanalyze`, débounce 60 ms, garde `REPL._building` pour ne pas s'auto-déclencher pendant le setup). Fini l'incohérence « spectre calculé avec l'ancienne fenêtre ». **Pondération A/C/Z** : toujours instantanée (pas de ré-analyse, appliquée à la volée depuis le brut). **Offset de calibration** : appliqué à l'affichage sans ré-analyse. *(Rappel limites en cours : Fast/Slow sans effet en replay — trames = instantanés ; spectrogramme-rembobinage et MOY bande fine = prochaine étape, nécessitent le stockage des spectres par trame.)*

- **2.0.18-beta** : **REWORK MAJEUR du replay — modèle « analyse à froid + index »**. Abandon du pilotage par l'horloge audio (source de toutes les dérives). Au chargement, `replBuildFrames(buf)` rééchantillonne le PCM à la fs du contexte puis découpe **tout le fichier en trames ~50 ms** analysées une fois (FFT → tiers d'octave bruts + A/C/Z instantanés, **recalage Parseval par trame**), avec **sommes cumulées (prefix)** pour lire la moyenne début→curseur en O(1). Progression affichée dans `#rbCur`. Ensuite le **slider = index** : `renderReplayFrame(i)` peuple `S.eq/eqT/bandEnergy` (MOYENNE début→i), `S.pF/bandPowF` (instantané au point), refait **une** FFT pour le spectre bande fine instantané, reconstruit `S.hist` (courbe L(t) 0→i sous-échantillonnée) → l'affichage **ne dépend plus jamais de l'horloge** (zéro dérive). **Lecture** = `replPlayTick` (rAF) qui avance l'index en temps réel + `replAudioTo` pour le son ; **toucher le slider met en pause**, glisser déplace l'index (affichage instantané, sans son), relâcher reste au point, **▶ reprend d'ici**, **stop → index 0**, **maintenir n'avance plus rien**. Slider et affichage sont le même index → toujours synchronisés. `setTransport`/badge pilotés par `REPL.playing`. **Limites connues** : MOY *bande fine* indisponible en scrub (repli instantané ; les **tiers d'octave** moyennés sont exacts) ; LCpeak = max des LC instantanés par trame (pas une vraie crête temporelle) ; spectrogramme alimenté pendant la lecture, figé (pas de rembobinage) pendant le scrub ; changer la correction micro nécessite de relancer le replay. Fonctions supprimées : `replaySeek/replPos/preview/commit/pendingSeek/scrub`.

- **2.0.17-beta** : **STOP remet le curseur au début**. À l'arrêt d'un replay (stop manuel ou fin naturelle), le slider revient à 0:00 (`REPL.startOff=0`, `pendingSeek`/`scrub` remis à null, `replTickUi(0)`) ; ⟲ ou un glissé repartent proprement du début.

- **2.0.16-beta** : **CORRECTIF mécanique du slider — l'horloge défilait pendant le scrub**. En maintenant le doigt sur la timeline, `S._previewRing` gardait la boucle active et `S.elapsed+=dt` + `S.hist.push` + `sgTick()` continuaient de tourner alors que l'audio était gelé (ctx suspendu) → **décalage temps** et spectrogramme qui défilait tout seul au lieu de refléter la position. Correctif : pendant le preview, (1) `S.elapsed` n'avance plus (`if(!S._previewRing)`), (2) branche preview dédiée qui met à jour **uniquement la vue fréquentielle** (spectre du point sous le doigt) puis `return` **avant** historique / spectrogramme / auto-stop. L'horloge et le spectrogramme sont donc figés tant qu'on scrube ; au relâcher on reste en pause au point visé, ▶ repart de là (inchangé). *(Limite connue : le spectrogramme ne « rembobine » pas encore pour montrer l'historique jusqu'au point visé — il reprend son défilement à la lecture ; reconstruction possible dans un chantier ultérieur.)*

- **2.0.15-beta** : **CORRECTIF — la demande d'autorisation micro au 1er REC ne se déclenchait plus**. Régression de la chaîne `micWanted` (2.0.10) : `prewarmMic` (préchauffage au 1er tap **n'importe où**) créait `S.ctx` avant tout REC → au tap REC, `S.ctx` existait déjà donc passage par `reviveMic` au lieu d'`initAudio`, et surtout le prompt `getUserMedia` n'était plus émis au bon moment. Correctif : `prewarmMic` **ne crée le contexte / ne demande le micro que si `S.micWanted`** (posé uniquement par REC ou ♪ SON) ; listener rendu **persistant** (plus `{once}`) pour re-préchauffer dès que le micro devient voulu. Résultat : app lancée → 1er REC → **prompt micro normal** ; les replays purs restent sans prompt.

- **2.0.14-beta** : **scrub = pause auto**. Toucher la timeline **met en pause** (bouton ▶ vert) : on explore sans que la lecture continue derrière. `grab()` au touchstart suspend si en lecture ; `commit` reste en pause au point visé (`pendingSeek=t`, `startOff=t`) sans relancer l'audio ; sur un replay **fini**, le premier mouvement reprend **gelé** (`replayStart(id,t,startPaused=true)` → `ctx.suspend()` après `src.start`). La reprise (▶) exécute le `pendingSeek` puis `resume` → lecture depuis le point exact. Le scrub visuel (courbes qui suivent le doigt) est inchangé, désormais toujours en contexte pausé.

- **2.0.13-beta** : **spectrogramme préservé au saut de timeline**. Chaque seek/scrub appelle `beginRun()` (indices repartent) qui **effaçait aussi le spectrogramme, la vue 3D et les vues** (règle « départ vierge » héritée de la mesure micro) → sensation de « ça recommence tout ». Le nettoyage visuel est isolé dans `clearRunVisuals()` et **n'est plus appelé par `beginRun` en replay** (`if(!REPL.on)`) : en replay le spectrogramme reste un flux continu de ce qu'on explore ; le nettoyage complet n'a lieu qu'au **démarrage** d'un replay (`replayStart` → `clearRunVisuals()` explicite) ou d'une mesure micro. *(Note process : les commits `beta/index.html` portaient tous le libellé figé « build 2.0.9-beta » — contenu correct, étiquette erronée ; corrigé, le message reflète désormais la version.)*

- **2.0.12-beta** : **scrub visuel refondu (le 2.0.10 ne marchait pas)**. Les « sauts réels throttlés » ne pouvaient PAS suivre : le tampon de l'AnalyserNode (fftN, ~0,7 s à 32k) + lissage Fast >> 140 ms de throttle → spectre quasi figé. Nouveau chemin : **`replPreviewFill(t)` remplit directement `S.ring`** avec les N échantillons du PCM finissant au point sous le doigt (rééchantillonnage linéaire si fs enregistrement ≠ fs contexte), `S._previewRing=true` fait sauter la lecture analyser dans `computeSpectrum` — **la boucle d'affichage normale fait tout le reste à 60 fps** (spectre, tiers, tout suit le doigt). Marche **aussi en pause** (`loop` : `if(S.paused&&!S._previewRing)return`). **Zéro pollution des indices** : `idle` inclut `_previewRing` (affichage seul, aucune intégration pendant le drag). Plus aucune recréation de source pendant le glissé — le saut audio réel se fait au relâcher (inchangé : seek / pendingSeek en pause / relance après fin). Flag coupé au commit, au touchcancel et au teardown.

- **2.0.11-beta** : **écoute filtrée (♪ SON) contextuelle**. En **replay**, elle joue le **son rejoué** (source = `REPL.src`), sans jamais demander le micro ; en **mesure**, le micro (inchangé). `lsnSrc()` choisit la source, `lsnBuild` mémorise `srcRef` (déconnexion propre), et **`lsnRetarget(src)`** rebranche la chaîne : appelé à chaque **seek/relance** du replay (la nouvelle `AudioBufferSourceNode` remplace l'ancienne) et au **teardown** (retour micro s'il existe, extinction sinon). Barre finie + tap ♪ SON → toast « lance la lecture » (pas de prompt micro). Message d'activation adapté en replay (pas d'avertissement larsen : aucun micro ouvert). En pause, la chaîne se construit et sonne à la reprise.

- **2.0.10-beta** : **micro à la demande + scrub live**. (1) **Micro demandé UNIQUEMENT par un geste qui l'exige** (REC ou ♪ SON) via `S.micWanted` ; `reviveMic` refuse tant que ce drapeau n'est pas posé → **plus aucun prompt micro après un replay pur** (retour d'arrière-plan compris). Rejouer les enregistrements sauvegardés ne demande jamais le micro ; une fois activé, il reste dispo pour enregistrer quand on veut. (2) **Scrub live** : pendant le glissé de la timeline, **sauts réels throttlés (~7/s, 140 ms)** → la vraie chaîne analyse le point sous le doigt, **les courbes suivent le slider** (recherche visuelle d'un moment à rejouer ; le replay étant silencieux — branché sur l'analyseur, pas la sortie — pas de bruit de scrub). Après la fin : le premier mouvement relance (buffer conservé, garde `starting` anti-doubles). En pause : affichage seul + saut mémorisé (inchangé). Chaque pas de scrub = `beginRun` (mode recherche assumé : les indices repartent).

- **2.0.9-beta** : **zones mortes autour des icônes superposées**. Un tap qui vise (ou frôle à < 10 px) un bouton posé sur un graphe (3D, LSN, MOY, ÉMG…) ne déclenche plus le canvas en dessous (curseur/zoom intempestifs, surtout spectrogramme 3D). Helper `overOverlayBtn(e,cv)` (rect élargi M=10 px des boutons **visibles** du même panneau) inséré en garde en tête des **4 gestionnaires** de gestes canvas (`attachZoomPan` touchstart+click, rotation 3D, 4e canvas) — return AVANT `preventDefault`, le bouton reçoit son tap normalement. **Correctif porté sur main (1.35.46)** : bug présent en V1 aussi.

- **2.0.8-beta** : **barre vivante après la fin**. Lecture finie (ou stoppée), glisser la barre **relance le replay au point visé** et les graphes repartent — équivalent lecteur audio. Pour ça : `replayStart(id, startAt)` (démarrage à un offset, `src.start(0,startAt)`), le **buffer et les meta sont CONSERVÉS au teardown** tant que la barre est affichée → relance **instantanée** sans relire IndexedDB ni redécoder ; libérés dès qu'une mesure micro démarre (`ui()`). Gestes de la piste : `canScrub()` = replay actif OU (fini + barre visible + `lastId`) ; commit → seek (lecture), pendingSeek (pause) ou `replayStart(lastId,t)` (fini).

- **2.0.7-beta** : **bouton pause ⇄ lecture**. En pause, la touche affiche **▶ vert** (triangle `ICO.play`, fill `#1E7A46` + halo vert, pulse conservé) sur le fond ambre enfoncé ; en lecture, **⏸**. Le bouton montre l'**action à venir**, pas l'état — inverse le choix historique « ⏸ reste ⏸ » (commentaire retiré) sur demande. Vaut pour mesure ET replay (même touche).

- **2.0.6-beta** : **timeline — retours terrain**. (1) **Saut en pause = différé** : relâcher la poignée pendant la pause **mémorise** la cible (`REPL.pendingSeek`, affichage figé dessus) sans rien relancer ; le saut s'exécute **à la reprise** (seek pendant suspend puis `resume` → départ pile au point choisi). La lecture ne redémarre jamais toute seule. (2) **Anti-souris fantôme iOS** : les événements souris simulés émis ~300 ms après un toucher déclenchaient un second saut → ignorés (< 700 ms après un touch), `preventDefault` sur touchend. (3) **⟲ Rejouer** en bout de barre : à la fin (naturelle ou stop), la timeline **reste affichée** avec le bouton ⟲ → relance immédiate du même enregistrement (`REPL.lastId`), sans repasser par le menu export ; barre masquée dès qu'une mesure micro démarre. Harnais bibliothèque rejoué OK.

- **2.0.5-beta** : **CORRECTIF de régression 2.0.4** — le retrait de l'import externe avait **supprimé par erreur 5 fonctions** de la bibliothèque (`recList/recDelete/recRename/recSegs/recWavBlob`) : la sauvegarde marchait (`recPut` intact, **les données des utilisateurs sont en base et réapparaissent**) mais liste, WAV, replay et suppression étaient morts (« bibliothèque indisponible »). Fonctions réinsérées, **test harnais fake-indexeddb rejoué OK**. Leçon procédure : **rejouer `rdb_test.js` avant chaque déploiement V2** (la 2.0.4 était partie sans). + **Diagnostic stockage** dans la liste : persistance réelle (`storage.persisted()`), usage/quota, contexte (Safari vs app installée) + chemin ; erreurs IndexedDB affichées en clair (plus de catch muet). + **Sauvegarde vérifiée par relecture** (meta + compte de segments) avec rappel « attendre ~10 s avant de fermer » (durabilité d'écriture WebKit).

- **2.0.4-beta** : **chantier 4 — timeline de replay**. Barre `#replBar` au-dessus du strip bas, visible seulement en replay : temps courant · piste scrubable (fill + poignée, `touch-action:none`) · durée. Position = `startOff + (ctx.currentTime − startCt)` (le `suspend` de la pause **fige** currentTime → la position gèle proprement). **Drag = viser** (affichage seul, `REPL.scrub`), **relâcher = saut** : `replaySeek(t)` recrée l'`AudioBufferSourceNode` à `start(0,t)` (pas de seek natif), l'ancien étant neutralisé (`onended=null` avant stop ; l'`onended` du neuf vérifie `REPL.src===src` contre les stops fantômes). **Au saut, `beginRun()`** : les intégrateurs repartent au point choisi — les indices couvrent « depuis le dernier saut », jamais un patchwork (choix d'honnêteté métrologique) ; `S.lcLastCt` réamorcé. Tick d'affichage 200 ms (`REPL.tick`), nettoyé au teardown avec le buffer. Pas de vitesse variable (un `playbackRate` fausserait l'analyse) — chantier séparé si besoin. + factorisation `recPut`/`buildSegs` (préparation d'un éventuel import externe, non exposé).

- **2.0.3-beta** : **chantier 3 — moteur de replay**. Un enregistrement de la bibliothèque se **rejoue dans la chaîne d'analyse** comme si c'était le micro : `AudioBufferSourceNode` (PCM int16 → AudioBuffer à la fs d'origine, **rééchantillonné par WebAudio** si besoin) branché sur `S.analyser` à la place du micro (déconnecté, rebranché à la fin). Tout le pipeline tourne à l'identique — spectre, Leq/MOY, émergence, **LCpeak et banc IIR inclus** (alimentés depuis `S.tap`). **Calibration du jour de la mesure** : `meta.offset` appliqué pendant le replay puis restauré. **Pas d'armement** (signal numérique propre : warm/délai/seuil à zéro, `beginRun()` immédiat, `S.lcBq` réamorcé à la fs du contexte). Pause = `ctx.suspend()/resume()`. Fin naturelle (`onended`) ou stop manuel → `stopMeasure` → `replayTeardown()` (offset restauré, micro rebranché + `arecWire`). **Gardes** : chien de garde micro (`micAlive`) neutralisé en replay ; capture AREC jamais active pendant un replay ; `beginRun` ne reset pas la capture en replay. UI : bouton **▶** par enregistrement, badge **REPLAY** bleu (`.badge.replay`), « REPLAY FINI · Leq figé » à l'arrêt. Fonctionne **sans micro** (contexte audio créé à la volée — usage bureau). Limites connues (chantier 4) : vitesse ×1 uniquement, pas de timeline/scrub ; le profil de correction fréquentielle n'est pas re-appliqué (seul l'offset l'est) si le profil actif diffère de celui de l'enregistrement.

- **2.0.2-beta** : **chantier 2 — bibliothèque locale IndexedDB**. Base `sono_rec` : store `recs` (métadonnées : nom, date, fs, durée, len, **offset + profil de calibration**, version app — le nécessaire au replay) et store `chunks` (PCM int16 consolidé en segments ~1 Mo, clé `[id,seq]`, jamais toute la bibliothèque en RAM). `navigator.storage.persist()` demandé à la première sauvegarde (anti-purge Safari). API : `recSave/recList/recRename/recDelete/recWavBlob(id)` (suppression par `IDBKeyRange.bound([id,0],[id,1e9])`). UI popover export : bouton **Sauvegarder** + liste **ENREGISTREMENTS LOCAUX** (nom, date · durée · taille · fs ; actions WAV / renommer / supprimer ; usage stockage affiché). **Testé sous Node avec fake-indexeddb** : cycle save→list→WAV (PCM vérifié octet par octet)→rename→delete OK. `toast()` générique ajouté (réutilise `#err`).

- **2.0.1-beta** : **chantier 1 — capture audio + export WAV**. Captage **continu** par `ScriptProcessorNode(4096)` dédié (`AREC`, l'AnalyserNode d'affichage perd des échantillons entre frames), raccordé à un gain muet vers destination (obligatoire iOS), recâblé à chaque `acquireMic()`. Blocs **Int16** empilés uniquement pendant la mesure effective — mêmes règles que le Leq : rien si `!running`, `paused` ou `S._idle` (miroir du drapeau d'armement posé dans updateNumbers). Reset à chaque `beginRun` si actif. **Garde-fou 20 min** (~115 Mo, arrêt propre + état « plein »). UI : popover export, section **AUDIO · V2 BÊTA** (toggle persisté `recAudio`, état durée/taille/fs, boutons Partager/Télécharger WAV). Encodeur **WAV PCM16 mono** (en-tête RIFF 44 o, fs réelle du contexte). Splash `2.0.1-beta` ; pas de SW sur /beta/.

## Consignes — passage beta test -> version officielle (production main)

Quand on promeut la beta (branche V2) en version officielle a la RACINE de `main` :

1. **La mention « beta » de la version n'existe qu'a UN seul endroit** : le libelle `#splashVer` (ex. « 2.0.89-beta », ligne ~571). Tout le reste en DERIVE : `APP_VERSION` = texte de #splashVer (ligne ~4485), utilise dans les exports/sauvegardes (`app:APP_VERSION`, ligne ~1836) et l'affichage « v »+version (hpVer, ligne ~4486). Donc, pour la prod : mettre #splashVer a « X.Y.Z » **sans** « -beta » -> aucun export/sauvegarde ne portera « beta ».
2. **La branche V2 et le canal `/beta/` GARDENT le « -beta »** (c'est voulu : ce sont les canaux de test). Ne pas les « nettoyer ».
3. **NE PAS toucher** a la detection du canal `location.pathname.includes('/beta/')` (lignes ~20-31) ni a la cle sessionStorage `betaPurged` : c'est FONCTIONNEL (ici « beta » designe l'URL du canal de test, pas la version), invisible pour l'utilisateur.
4. **Recaler le cache SW** (nouveau nom `sono-<ver>-<hash>`) pour forcer la mise a jour (reseau-d'abord -> bascule des utilisateurs).
5. **Verifier apres promotion** (API `contents/index.html?ref=main`) : #splashVer sans « beta » ; et un export de test ne doit pas contenir « beta ».
6. Les enregistrements **deja sauvegardes sur la beta** gardent leur estampille `app:...-beta` (donnees historiques, non reecrites) ; les NOUVEAUX sont propres.

`promote_main.py` applique automatiquement les points 1 et 4 (splashVer -beta -> propre + recalage cache).

## 8ter. Journal des versions (V1 / main)

- **PROMOTION 2.0.89 (production)** : la branche V2 (ex-beta) est promue en PRODUCTION a la racine de `main` (version affichee **2.0.89**, sans -beta ; cache SW `sono-2.0.89-<hash>`, reseau-d'abord -> les utilisateurs V1 basculent sur V2 a la prochaine visite en ligne). L'ancienne V1 (1.35.x) reste dans l'historique git (restaurable via deploy_v1.py) ; la branche V2 et le canal /beta restent inchanges pour le developpement continu. Ref rapport de validation : SONO-VAL-000.

- **1.35.46** : **zones mortes autour des icônes superposées** (portage du correctif 2.0.9-beta) : un tap visant un bouton posé sur un graphe ne pose plus de curseur ni ne déclenche le zoom du canvas en dessous (garde `overOverlayBtn`, marge 10 px, 4 gestionnaires).

- **1.35.45** : **infra canal beta V2**. Le service worker de main **exclut les chemins `/beta/`** de son interception (sinon il écrasait le cache `./index.html` de la V1 avec la page beta en ligne, et servait la V1 sur /beta/ hors-ligne). Aucun changement fonctionnel de l'app V1 (bump de version pour la traçabilité et la diffusion du SW).

> Les versions antérieures à V27 sont documentées dans l'historique Git.

- **1.35.44** : **versionnage sémantique** `MAJEUR.MINEUR.PATCH` (V35.43 → 1.35.44 ; 2.x réservé au prochain gros changement). Source unique = splash `#splashVer`, lu par `APP_VERSION` et affiché dans le **titre de l'aide** (`#hpVer`, badge « v1.35.44 »). Rapport de validation régénéré avec la nouvelle version. **Icône micro d'en-tête cliquable** (`#micStat`, cursor:pointer) : tap → **petit menu CALIBRATION enrichi** d'un bloc MICRO (nom de l'entrée active `#cpMicName`, **fréquence d'échantillonnage** `#cpMicRate`, **sélecteur** `#micSelPop`). Factorisation : `applyMicChoice(sel)` partagé par les deux selects (écran cal + popover), `populateMicList()` remplit les deux, `cpShowMic()` rafraîchit nom+fs (aussi après changement de micro).
- **V35.43** : **mode MOY — curseurs et émergence cohérents avec la courbe**. En bande fine, la courbe moyennée était tracée depuis `binEnergy/eqT` mais `getDb` (curseur tap), le **curseur max deux-doigts** et l'**émergence** lisaient encore l'instantané lissé. Correctif : **source effective unique `binPowEff`** (tampon persistant `S._avgBinPow=binEnergy/eqT`, recalculé par frame sans allocation) utilisée par le tracé, `getDb`, l'émergence au curseur et le max. **Encadré orange en MOY** : détection **directe** de la raie dominante sur le spectre moyenné (stable par nature → pas de confirmation temporelle) via `tonalEmergence(binPowEff,…)` ; en live, comportement confirmé/hystérésis inchangé. Banc de validation rejoué : OK. + **Rapport de validation docx publié dans le dépôt** (`tests/Rapport_validation_DSP_sonometre_CETIM.docx`, réf. SONO-VAL-001).
- **V35.42** : **exports moyennés + résolution + VIB retiré de l'UI + réagencement icônes**. (1) **Tous les exports du spectre** (rapport PDF, PNG, CSV) sont en **Leq moyenné** sur la mesure. Nouveau helper `expSpectrum(band)` (tiers ou agrégat octave depuis `bandEnergy/eqT`) utilisé par la table du rapport et le CSV ; `drawSpectrum(avg,modeOverride)` accepte un override de résolution, et `figureDataURL('spec')`/`buildGraphCanvas` forcent `drawSpectrum(true,S.expBand)` (le PNG re-rend puis restaure l'écran). (2) **Option octave / tiers d'octave à l'export** : `S.expBand` ('toct'|'oct', persisté), radios dans le popover export (synchronisés à l'ouverture), pilote figure + table + CSV + PNG indépendamment de l'affichage écran. (3) **Module VIB retiré de l'interface** : bouton `#vibBtn` masqué (`display:none` + forcé dans `ui()`), overlays inactifs (VIB.on reste false) ; **code conservé** (fonctions `vibToggle`/`vibCompute`… intactes, réactivable) ; mentions d'aide FR/EN/ES remplacées par MOY. (4) **Icônes du spectre réagencées** : **MOY** tout à droite (`right:8`, tous modes), **ÉMG** à sa gauche (`right:60`, bande fine uniquement), VIB supprimé de la rangée.
- **V35.41** : **spectre instantané ⇄ moyenné (Leq)**. Nouvel accumulateur d'énergie par bin FFT `S.binEnergy` (Float64, en miroir de `bandEnergy` pour les tiers d'octave) : `+= p·dt` quand `!idle`, remis à zéro dans `clearAll` et `beginRun`. Nouveau toggle écran **MOY** (`#avgBtn`, à côté de VIB/ÉMG, `right:112px`) piloté par `S.specAvg` (persisté) : bascule l'affichage du spectre entre instantané (lissage TEMPS Fast/Slow/V‑Slow) et **moyenné Leq sur la durée de mesure**, en tiers d'octave, octave ET bande fine. `drawSpectrum(avg)` : `useAvg = (avg===undefined?S.specAvg:avg) && eqT>0` → source `bandEnergy/eqT` ou `binEnergy/eqT`, peak-hold masqué, libellé « L<x>·Moy ». Le **rapport PDF** force toujours la figure moyennée (`figureDataURL('spec')`→`drawSpectrum(true)`) — **corrige le défaut** où le spectre exporté montrait l'instant final au lieu de la moyenne. Bouton grisé tant qu'aucune mesure (`eqT=0`). TEMPS et max/min/percentiles **inchangés** (pas d'ambiguïté). Aide EN/ES du bouton MOY à compléter.
- **V35.40** : **rapport — pagination + saisie**. (1) **Un graphe par page** : chaque figure sélectionnée est enveloppée dans `.rep-fig` avec `break-before:page` + `break-inside:avoid` (impression), titre en haut de page, image bornée `max-height:225mm object-fit:contain`. Page 1 = en-tête + indices + conditions ; figures ensuite chacune sur sa page ; la dernière partage sa page avec le tableau tiers d'octave + commentaire. Sur écran (pas d'impression) les figures s'enchaînent normalement. (2) **Popover export** : champ **Commentaire agrandi** (2→4 lignes, `min-height:76px`, `resize:vertical`) ; scroll **confiné** (`overscroll-behavior:contain`) ; **scroll-into-view au focus** des champs (`e.scrollIntoView({block:'center'})` après 300 ms) → le clavier iOS ne masque plus le champ en cours de saisie (surtout le commentaire).
- **V35.39** : **en-tête rapport responsive** (correctif largeur sur l'aperçu écran étroit). Le rapport est conçu A4 mais s'affiche à ~390 px sur iPhone → le logo (~190 px) écrasait titre et méta en cascade. `.rep-h` passe en `flex-wrap`, `.rep-htxt` en `flex:1 1 auto;min-width:0`, et une **media query `screen and (max-width:600px)`** (n'affecte PAS l'impression/A4) : logo 30→22 px, titres réduits, `.rep-htxt{flex-basis:0}` (logo+titre restent sur une ligne, le titre s'enroule), **bloc méta en pleine largeur sous le titre** (`order:3`, `flex-basis:100%`, aligné à gauche, lignes `label valeur` en flux `white-space:nowrap`). Le PDF final (media print) est inchangé.
- **V35.38** : **en-tête du rapport PDF refondu** (charte CETIM). Le CSS définissait `.rep-bar` (trait rouge) et `.rep-t1` (titre navy) **inutilisés** → header remis en cohérence avec l'app : logo CETIM navy · **séparateur rouge vertical** (`.rep-bar`, `align-self:stretch`) · **titre fort** navy capitales `.rep-t1` « Rapport de mesure acoustique » + sous-titre `.rep-t2` « Sonomètre CETIM · analyse de bruit ». Bloc **méta** à droite structuré en lignes `LABEL valeur` (Date/Réf/Site/Opérateur, `.rl` gris capitales, **même corps que la valeur** — 10 px), n'affiche que les champs remplis. Filet navy 2 px sous l'en-tête + **accent rouge** (`.rep-h::after`, 66 px à gauche). Logo rapport 32→30 px.
- **V35.37** : **logo d'en-tête** ramené de 18 à 15 px (`.logo`) → largeur ~114→95 px, aligné en hauteur sur le wordmark « Sonomètre » (le trait rouge séparateur ne paraît plus court). **Curseur FFT** : fenêtre d'accroche du pic au tap élargie de **±2 % → ±4 %** (plancher 2→3 bins) — le tap accroche un pic un peu plus loin autour du point touché ; curseur deux-doigts (max de toute la fenêtre zoomée) inchangé.
- **V35.36** : **curseur FFT** — (a) **double-tap sur le curseur** l'enlève (`opts.onCursorRemove`, détecté quand le second tap tombe sur le curseur ; double-tap ailleurs = reset vue comme avant) ; (b) **accroche conditionnelle** : le tap **au-dessus** de la courbe accroche le pic local (fenêtre ±2 % + interp. parabolique), le tap **sous/sur** la courbe = placement libre à la fréquence visée (`S.cursor.snap` décidé via `tapDb > specCurveDb(f)+1` ; `tapDb` calculé dans `touchend` par l'inverse de `y2` ; le glissé conserve le mode, `tapDb==null`). Libellé ⟂ seulement si accroché. **Logo** : en-tête rapport aligné (logo seul + sous-titre, sans doublon « SONOMÈTRE »), PNG logo 16→18 px. (1) **vrai logo CETIM** dans le rapport (en-tête, `cetimLogoSVG(fill)` reconstruit le SVG depuis le logo du DOM — chemins exacts, lettres `#001E50`, carré rouge) et dans l'export PNG (`LOGO_IMG_LIGHT` pré-rasterisé en `#EAF4FF`, `drawImage` avec repli texte). (2) **Libellé corrigé** : « correction d'amplitude » → **« Réf. pleine échelle (0 dBFS) »** dans le rapport, le menu expert (`mCalib`), le titre écran calibration et l'i18n (l'offset ~120 dB est le niveau à 0 dBFS, pas une correction). (3) **Zoom vertical du spectre** ancré **sous le milieu des doigts** (`anchors.yc` mémorisé au verrouillage db, dilatation autour de `anchorDb`) → fini la dérive haut/bas. (4) **Curseur spectre déplaçable au glissé** : `attachZoomPan` détecte la prise sur le curseur au `touchstart` (`opts.getCursorU`, seuil 5 % de largeur) → `mode='cursor'`, et le `touchmove` appelle `opts.onTap` en continu (comme le curseur d'évolution).
- **V35.34** : rapport multi-figures (cases Spectre/Spectrogramme/Évolution, rendu hors écran via `figureDataURL`).
- **V35.33** : **rapport PDF fiabilisé** — onglet autonome (`window.open`+`document.write(buildReportDoc)`), `REPORT_CSS`, impression auto au `load`, repli partage `.html` si popup bloqué. (Remplace l'ancienne impression `@media print` qui imprimait l'app.)
- **V35.32** : **refonte du menu Export → rapport pro**. Popover réorganisé en 3 blocs : (1) **Infos du rapport** facultatives et mémorisées en `localStorage` (`rep_repSite/Oper/Ref/Note`) ; (2) **Rapport complet (PDF)** ; (3) CSV/PNG conservés. Le rapport A4 contient : en-tête CETIM + date/réf/site/opérateur, tableau **indices** (LAeq/LCeq/LZeq, LAFmax/min, LCpeak, L10/50/90, LAE-SEL, ΔLF, durée), **conditions d'acquisition** (micro, Fe, FFT, fenêtre, overlap, source 1/3 oct, pondérations, correction d'amplitude `S.offset`, écran exporté), image du graphe courant, **spectre par tiers d'octave**, commentaire, pied **non-métrologique**. CSS `.ep-in`. Aide FR MAJ.
- **V35.31** : **3D — axe temps** : le marqueur « ▲ présent » est remplacé par un vrai **axe temps** sur la profondeur (étiquettes `0 s` au front / `-winSec/2` / `-winSec` au fond, guides + titre « temps » vertical à gauche ; `marginL` 14→38 pour loger les étiquettes). **Rotation élargie** : plus d'amplitude autour de l'axe des fréquences (`depth=plotH*(0.40+0.48*pitch)`, `ampH=…*(1-0.26*pitch)`, geste `dy/220`) et azimut plus symétrique (`skewTot=plotW*(0.12+0.50*rot)`). **Export** (`buildGraphCanvas`) : en-tête sans chevauchement — titre + sous-titre **empilés à gauche** (y 29 / 47), date **en haut à droite** (y 29), `top` 52→56 ; le sous-titre spectrogramme indique aussi la **durée de fenêtre** (ex. « · 5 s · 3D »).
- **V35.30** : **3 niveaux de lissage** seulement — `S.sg3dSmooth` 0-2 = **Standard / Lisse / Très lisse** (défaut **Lisse** = milieu ; `sg3dSmooth:1`). Niveau « Fluide » supprimé → `maxFrames=96` et `maxSlices=90` toujours ; `wirePass=smoothLvl` (0/1/2). Cycle `%3`, clamp `Math.min(2,…)`. **Bug relancement corrigé** : `resetSg3d()` (vide `window.V3.frames/ema/acc/lastT` + `SG._3dKey/_3dLast`) appelé dans `clearAll`, `clearSgram` et le reset de mesure — on ne revoit plus l'ancienne trace 3D. **Perf mesure longue** : pool de tampons persistant `SG._sc={pool,slice,norm,raw,dz}` → `smoothProf(prof,out)` écrit dans un tampon fourni, `liveEMA` réutilise `raw`, front resynchronisé par `.set()` (plus de `slice()` par image), `dzArr` réutilisé. Fin des ~20 000 `Float32Array/s` → plus d'à-coups GC après quelques minutes. `sg3dSmooth` persisté clampé (ancien 3→2).
- **V35.29** : **front live continu** — la courbe de front (présent) suit maintenant le signal à **chaque image** (EMA à constante de temps ~55 ms, resynchronisée sur le dernier frame chaque redraw) au lieu d'un instantané figé rafraîchi seulement à la cadence de capture. Fin du léger « saut de haut en bas » du front (surtout visible en HF/à droite). Push d'un instantané figé à `capInt` (l'ancien front est committé, un nouveau front live repart).
- **V35.28** : anti-scintillement du fil de fer 3D — cadence remontée à **~60 FPS** (le rendu par dégradé, 1 tracé/courbe, est désormais assez léger ; pas de glissement deux fois plus petits) et **fondu du bord arrière** (`edgeA` selon `dz`) : la courbe la plus ancienne s'estompe au lieu de disparaître d'un coup. Dégradé allégé (1 point sur 2).
- **V35.27** : le **spectrogramme 2D** retrouve l'option **« Tout »** (défaut 2D) ; le **3D** garde 3/5/10 s (défaut 5 s, jamais « Tout »). Fenêtres de temps **mémorisées par vue** (`S._tv2d`/`S._tv3d`) basculées au toggle 2D↔3D. `sgDefaultView()` (2D→Tout, 3D→5 s) utilisé par tous les resets. **Appui long ÉCHELLES** applique le défaut selon la vue, libellé passe bien à **« Auto »** (`fullT` du libellé : 5 s en 3D, Tout en 2D).
- **V35.24–26** : bouton LOG/LIN aligné puis élargi (hauteur 42 px = bouton OK, largeur 92 px) dans la ligne de saisie Hz.
- **V35.23** : toggle **LOG/LIN** déplacé dans la ligne de saisie Hz (ne mord plus le stepper max) ; **mode surface entièrement retiré du code** (fil de fer uniquement) — le niveau max de « Lissage / perf. 3D » devient **Très lisse** (Fluide/Standard/Lisse/Très lisse) ; **traduction corrigée à la racine** : `trDyn()` traduit toujours depuis le français mémorisé avec garde `dataset.tr` → fini le recompoundage « expertoooooo » et la bascule ES→EN fonctionne (plus de « Compartir/Réinit » figés).
- **V35.22** : toggle **LOG/LIN** (échelle fréquence) déplacé du menu expert vers le popover **ÉCHELLES** (section fréquence, présent pour spectre + spectrogramme) ; steppers dB resserrés (le « + » du max ne déborde plus) ; **mode surface retiré** du menu — il devient le **niveau maximal** d'une nouvelle option expert **« Lissage / perf. 3D »** (Fluide = moins de courbes pour smartphone à la peine → Standard → Lisse → Surface) ; `S.sg3dSmooth` (0–3) pilote maxFrames, passes de lissage fil de fer et bascule surface ; bouton Interpolation compact.
- **V35.21** : saisie **fréquence** (et temps) appliquée sur `change` en plus du bouton OK (le 1ᵉʳ appui OK ne faisait que fermer le clavier iOS → « ça ne fait rien ») ; **Pleine bande** = 20 Hz–20 kHz, détection d'état corrigée (comparait à Nyquist au lieu de `min(20000,nyq)`) et n'altère plus le temps ; couleurs du **fil de fer** lissées en **dégradé** le long de la courbe (fini les sauts de couleur marqués aux pics) et un seul `stroke` par courbe (plus léger).
- **V35.20** : options TEMPS spectrogramme = **3/5/10 s** (plus de « Tout » ; défaut **5 s** ; « Tout » conservé pour l'évolution) ; ÉCHELLES Auto met le temps à 5 s ; amplitude spectrogramme par défaut **fixe 0–90 dB** (preset 0–90 ajouté) ; **pause fige** le glissement 3D (capture/avance conditionnées à `!S.paused`) tout en laissant tourner/incliner ; **tangage** vertical (`S.sg3dPitch`, glisser bas = plongée) = rotation légère autour de l'axe des fréquences.
- **V35.19** : glissement continu réservé aux fenêtres bornées **3/5/10 s** ; en mode **Tout** retour à l'ancien rendu décimé léger (le glissement était saccadé/lourd sur tout le tampon). Helper `smoothProf` partagé.
- **V35.18** : fil de fer 3D à **glissement continu** — buffer de courbes-instantanés (`window.V3`) capturées à cadence régulière + EMA, profondeur continue interpolée par phase fractionnaire (fini le saut de tranche en tranche), redraw chaque frame en marche, plafond ~43 FPS. Look/masquage/couleurs inchangés.

- **V27–V33** : vue 3D waterfall (rotation, cache de rendu, modes fil de fer/surface, anti-scintillement), colorbar 3D unifiée avec la 2D, logo CETIM vectorisé, thèmes Game Boy et Matrice, modules VIB (corrélation vibro-acoustique) et écoute filtrée (🔊), traduction FR/EN/ES.
- **V33.1** : icônes ▶⏸ préservées lors de la traduction ; encadrés curseur/émergence sous les boutons ; traduction robuste (try/catch + re-passe).
- **V33.2–33.6** : estompage auto des icônes de graphes ; unités Hz/dB uniformisées et dessinées en dernier plan ; icônes du spectrogramme placées, dimensionnées et alignées horizontalement à gauche.
- **V33.7** : touche ANALYSE grisée hors de l'écran Spectre.
- **V34** : boutons transport style enregistreur ; correctif cache (`cache:'reload'`).
- **V34.1** : émergence toujours active (tableau + curseur), bouton ÉMG = encadré auto seulement.
- **V34.2** : bouton principal rouge (convention REC) ; service worker auto-actualisant.
- **V34.3** : icône statique du bouton START corrigée (triangle → point d'enregistrement).
- **V34.4** : thème appliqué avant le premier rendu (fin du flash) ; garde du reload SW.
- **V34.5** : suppression du grisage `mic-wait` au lancement.
- **V34.6** : masquage écran + clavier pendant le boot.
- **V34.7** : suppression du reload automatique du service worker (cause du saut de config à chaque mise à jour).
- **V35** : splash screen CETIM (logo, wordmark, barre de progression), fondu après durée minimale.
- **V35.1** : numéro de version affiché sur le splash, durée allongée à 1,4 s.
